/**
 * Hand-managed type defs (originally seeded from _research/dnd5e/module/applications/journal/_types.mjs). Edit directly — the JSDoc generator is retired.
 */

declare global {
  namespace dnd5e.types.applications.journal {
    interface JournalEditorConfiguration {
      /** The path to the specific HTML field being edited. */
      textKeyPath: string;
    }
  }
}

export {};
