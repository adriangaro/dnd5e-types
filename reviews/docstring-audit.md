# Docstring & comment fidelity audit — 2026-06-23

Audited **543/543** `.d.mts` files against their dnd5e runtime counterparts (one sonnet agent/file). Read-only audit; no edits applied.

## Totals
- **803 docstring fidelity issues** across 283 files
  - by severity: high 71 / medium 418 / low 314
  - by kind: missing 521 / altered 238 / fabricated 44
- **36 spurious/conversion-noise comments** flagged for removal
- 54 files have no runtime counterpart (pure types-infra)

## High-severity docstring issues (71)

- **module/_types.d.mts** — `TraitConfiguration.dataType` (altered): The runtime types this as `boolean|number` (primitive values used as flags), but our file changes it to `typeof dnd5e.dataModels.fields.MappingField | NumberConstructor` (constructor references). This is a semantic alteration — the runtime uses primitive type values, not constructor references here.
- **documents/scaling.d.mts** — `toString` (fabricated): Our declaration types toString() as returning number, but toString() must return string per TypeScript's built-in interface (Object.toString(): string). The runtime returns this.value and annotates value as @type {string}. The return type should be string (or string & number if a numeric string is intended, but plain string is correct). This fabricated number return type will cause a conflict with the built-in toString signature and mislead consumers.
- **documents/combatant.d.mts** — `getInitiativeRoll` (missing): The runtime overrides getInitiativeRoll but our declaration omits the method entirely. Add it with an @override doc noting it delegates to actor.getInitiativeRoll() when an actor is present, falling back to a plain D20Roll.
- **documents/token.d.mts** — `_initializeSource` (missing): The runtime defines _initializeSource (with a meaningful inline migration comment) but our file omits the override entirely. Add the member with at least an @inheritDoc.
- **documents/item.d.mts** — `dependentOrigin` (fabricated): Runtime getter returns a single ActiveEffect5e|null (the one effect whose rider created this item, via fromUuidSync of flags.dnd5e.dependentOn). Our declaration describes it as an array of documents and types it as foundry.abstract.Document.Any[], which is completely wrong — both description and type are fabricated.
- **documents/activity/mixin.d.mts** — `use` (missing): The `use` method has no doc comment. The runtime documents it as 'Activate this activity' with full param/return documentation. Port the runtime doc.
- **documents/activity/mixin.d.mts** — `consume` (missing): The `consume` method has no doc comment. Runtime documents it as 'Consume this activation's usage'. Port the runtime doc.
- **documents/activity/mixin.d.mts** — `rollDamage` (missing): The `rollDamage` method has no doc comment. Runtime documents it as 'Perform a damage roll' with full param/return docs. Port the runtime doc.
- **documents/activity/_types.d.mts** — `ActivityUseConfiguration.hasConsumption` (fabricated): The runtime `ActivityUseConfiguration` typedef has no `hasConsumption` property. This property exists on `ActivityMessageConfiguration` in the runtime (line 69). Our file adds it to `ActivityUseConfiguration` instead, which is fabricated. Remove it from `ActivityUseConfiguration` (it is already correctly present in `ActivityMessageConfiguration`).
- **documents/advancement/trait.d.mts** — `TraitAdvancement.maxTraits` (missing): The getter is not declared at all in our file. Add it with the runtime doc: '@type {number} — The maximum number of traits granted by this advancement. The number of traits actually granted may be lower if actor already has some traits.'
- **documents/advancement/trait.d.mts** — `TraitAdvancement.actorSelected` (missing): Method is not declared at all. Add with runtime doc and return type `Promise<{selected: Set<string>, available: Set<string>}>`.
- **documents/advancement/trait.d.mts** — `TraitAdvancement.representedTraits` (missing): Method is not declared. Add with runtime doc, param `pools?: Set<string>[]`, and return type `Set<string>`.
- **documents/advancement/trait.d.mts** — `TraitAdvancement.availableChoices` (missing): Async method is not declared. Add with runtime doc, param `chosen?: Set<string>`, and return type `Promise<{choices: SelectChoices, label: string} | null>`.
- **documents/advancement/trait.d.mts** — `TraitAdvancement.unfulfilledChoices` (missing): Async method is not declared. Add with runtime doc, param `chosen?: Set<string>`, and return type matching `TraitChoices` import from _types.mjs.
- **documents/advancement/mixin.d.mts** — `apply` (missing): Runtime has JSDoc with param docs including AdvancementApplicationData and options. This is a core method. Our declaration has none.
- **documents/advancement/mixin.d.mts** — `restore` (missing): Runtime documents the error-throwing behavior. Our declaration has none.
- **documents/advancement/mixin.d.mts** — `reverse` (missing): Runtime documents the return value (AdvancementReversalData for use by restore). Our declaration has none.
- **module/config/travelTypes.d.mts** — `Config.hidden` (altered): The runtime describes `hidden` as controlling whether the speed IS DISPLAYED (true = visible). Our doc inverts the semantics by saying 'is hidden', which implies true = hidden. Fix: 'Whether this movement speed is displayed on the actor sheet.' Also note runtime has a default of false ([hidden=false]).
- **module/config/ability.d.mts** — `Ability.Config.improvement` (fabricated): The `improvement` field does not appear in the runtime `@typedef AbilityConfiguration` in _types.mjs. It exists as ad-hoc data on the `hon`/`san` config entries but is not part of the declared type. Declaring it in `Config` as a typed property asserts runtime contract that the typedef does not provide.
- **module/config/limitedUsePeriods.d.mts** — `LimitedUsePeriod.DefaultTypes.encounter` (fabricated): The runtime limitedUsePeriods object does NOT contain an 'encounter' key. The nine runtime keys are: lr, sr, day, dawn, dusk, initiative, turnStart, turnEnd, turn. 'encounter' exists in activationTypes but not here — this entry is fabricated and will cause incorrect type narrowing.
- **module/config/encumbrance.d.mts** — `encumbrance.currencyPerWeight` (altered): The runtime doc says pieces of CURRENCY per unit weight (e.g. 50 coins = 1 lb). Our doc inverts this to 'weight per unit currency', which is the opposite ratio. Correct to match the runtime wording.
- **module/dice/basic-roll.d.mts** — `applyKeybindings` (altered): Our doc fabricates 'shift/ctrl/alt modifier keybindings' detail. The runtime doc says the method 'determines whether the roll process should be fast forwarded' — fast-forwarding is the actual purpose. The shift/ctrl/alt detail is not in the runtime doc.
- **module/dice/_types.d.mts** — `CriticalDamageConfiguration.powerfulCritical` (altered): Runtime types this as `string`, but our declaration uses `boolean`. The type is wrong, which is a load-bearing error — callers passing a boolean will pass type-checking incorrectly.
- **module/canvas/ability-template.d.mts** — `fromActivity` (missing): Our doc only has the @deprecated tag; the full factory method description, @param docs, and @returns are all missing. The runtime doc should be ported along with the deprecated notice.
- **module/data/actor/character.d.mts** — `addFavorite` (missing): Runtime method has a meaningful doc including @throws caveat. Our declaration has no doc comment. The @throws is load-bearing behavior documentation.
- **module/data/actor/npc.d.mts** — `resistSave` (missing): Runtime documents behavioral semantics (throws if no resistances remain, throws if not a save roll). Our declaration is completely undocumented. Port the runtime comment.
- **module/data/actor/vehicle.d.mts** — `adjustCrew` (missing): Runtime has a full JSDoc with param/returns. Our declaration has none. Port the runtime doc.
- **module/data/actor/vehicle.d.mts** — `getCrewUpdates` (missing): Runtime has a full JSDoc with param/returns. Our declaration has none. Port the runtime doc.
- **module/data/actor/vehicle.d.mts** — `getEncumbrance` (missing): Runtime has a JSDoc describing the method including the note about draft animals. Our declaration has none. Port the runtime doc.
- **module/data/shared/target-field.d.mts** — `TargetField.prepareData` (missing): The static method `prepareData(rollData, labels)` is not declared at all in the class body. It should be added as a static method with its full JSDoc (including @this, @param rollData, @param labels).
- **module/data/shared/target-field.d.mts** — `TargetField.templateDimensions` (missing): The static method `templateDimensions(type)` is not declared in the class body. It should be added as a static method with its JSDoc (param + return type matching the Dimensions interface already defined).
- **module/data/shared/uses-field.d.mts** — `UsesField.prepareData` (missing): The static `prepareData` method is entirely absent from the declaration file. It should be declared with its JSDoc: prepares formula values, derives `uses.value`, per-recovery `isScaleValue`/`recharge`, labels, and attaches `rollRecharge` to `this.uses`.
- **module/data/activity/enchant-data.d.mts** — `appliedEnchantments` (missing): The getter `appliedEnchantments` is declared in the runtime but entirely absent from our types file. Add it with its JSDoc: "Enchantments that have been applied by this activity."
- **module/data/activity/enchant-data.d.mts** — `availableEnchantments` (missing): The getter `availableEnchantments` is declared in the runtime but entirely absent from our types file. Add it with its JSDoc: "Enchantments that can be applied based on spell/character/class level."
- **module/data/activity/enchant-data.d.mts** — `enchantableTypes` (missing): The static getter `enchantableTypes` is declared in the runtime but entirely absent from our types file. Add it with its JSDoc: "List of item types that are enchantable."
- **module/data/activity/summon-data.d.mts** — `availableProfiles` (missing): The getter `availableProfiles` is not declared at all in our file. The file header notes it as a 'runtime getter, not schema-derived' but it still needs a typed declaration with its JSDoc so consumers can use it. Add `get availableProfiles(): SummonsProfile[]; /** Summons that can be performed based on spell/character/class level. */`.
- **module/data/activity/summon-data.d.mts** — `summonedCreatures` (missing): The getter `summonedCreatures` is not declared at all in our file. The file header mentions it but it needs a typed declaration with JSDoc. Add `get summonedCreatures(): Actor5e[];`.
- **module/data/activity/transform-data.d.mts** — `availableProfiles` (missing): The runtime getter has a meaningful JSDoc '@type {TransformProfile[]}' with description 'Transform profiles that can be performed based on spell/character/class level.' Our declaration has no doc on this getter. Port the runtime doc.
- **module/data/activity/base-activity.d.mts** — `ability` (missing): The `ability` getter is entirely absent from the declaration file. Runtime declares it with a meaningful doc. Add the getter and its doc.
- **module/data/activity/base-activity.d.mts** — `prepareData` (missing): The `prepareData()` method is entirely absent. Runtime defines it with a doc comment and it sets labels/indices on array fields.
- **module/data/activity/base-activity.d.mts** — `prepareFinalData` (missing): The `prepareFinalData(rollData?)` method is entirely absent. Runtime defines it with a `@param {ActivityRollData} [rollData]` doc.
- **module/data/activity/damage-data.d.mts** — `prepareFinalData` (missing): prepareFinalData(rollData) is implemented in the runtime (calls super.prepareFinalData + this.prepareDamageLabel) but is entirely absent from our declaration file. It should be declared so consumers know the override exists.
- **module/data/activity/damage-data.d.mts** — `getDamageConfig` (missing): getDamageConfig(config={}, options={}) is implemented in the runtime (merges critical.allow and critical.bonusDamage) but is entirely absent from our declaration file. It should be declared so consumers know the override exists.
- **module/data/item/_types.d.mts** — `WeaponItemSystemData.damage.bonus` (fabricated): The runtime @typedef WeaponItemSystemData has no damage.bonus property. This property does not exist in the runtime typedef and the entire sub-field is fabricated. Remove it.
- **module/data/chat-message/_types.d.mts** — `TurnMessageSystemData.trigger` (altered): The property is named 'periods' in the runtime, but our file declares it as 'trigger'. This is a wrong property name, not just a doc issue — the member itself is misnaming the field.
- **module/data/chat-message/_types.d.mts** — `UsageMessageSystemData.concentration` (fabricated): The runtime _types.mjs has no 'concentration' property on UsageMessageSystemData. This property and its doc are fabricated.
- **module/applications/dice/damage-configuration-dialog.d.mts** — `DamageRollConfigurationDialog._buildConfig` (missing): The method _buildConfig is entirely absent from the declarations. Runtime defines it as an @inheritDoc override that reads a damageType field from formData and sets config.options.type. It should be declared as a protected override.
- **module/applications/advancement/advancement-migration-dialog.d.mts** — `createDialog` (missing): The static createDialog method has no JSDoc in our file. The runtime documents all params, the return value, and a @throws. At minimum port the param descriptions and @throws annotation.
- **module/data/journal/map.d.mts** — `toEmbed` (missing): The runtime defines `async toEmbed(config, options={})` as a concrete `@override`. Our declaration file omits this method entirely. It should be declared as `toEmbed(config: unknown, options?: object): Promise<unknown>`.
- **module/data/active-effect/_types.d.mts** — `BaseActiveEffectSystemData.changes` (missing): The `changes` property is entirely absent from our declaration. Runtime defines it as `EffectChangeData[]` with doc 'Changes to apply to the actor.' — add the property with its doc comment.
- **module/data/active-effect/_types.d.mts** — `EnchantmentActiveEffectSystemData.changes` (missing): The `changes` property is entirely absent from our declaration. Runtime defines it as `EffectChangeData[]` with doc 'Changes to apply to the item.' — add the property with its doc comment.
- **module/data/calendar/_types.d.mts** — `CalendarFormattingContext.y` (altered): Runtime types y as string; our declaration uses number. Also adds "(unpadded)" which is not in the runtime description. Fix: change type to string and drop "(unpadded)".
- **module/data/calendar/_types.d.mts** — `CalendarFormattingContext.m` (fabricated): The runtime _types.mjs has no `m` property on CalendarFormattingContext. This member is entirely fabricated and must be removed.
- **module/data/calendar/_types.d.mts** — `CalendarFormattingContext.d` (altered): Runtime types d as string; our declaration uses number. Also adds "(unpadded)" which is not in the runtime description. Fix: change type to string and drop "(unpadded)".
- **module/canvas/api/base-placement.d.mts** — `BasePlacement.place (static)` (altered): Our description is fabricated/altered. Runtime says "Perform the placement, asking player guidance when necessary." — the key phrase "asking player guidance when necessary" is entirely dropped and replaced with unrelated wording.
- **module/canvas/api/base-placement.d.mts** — `place` (altered): Our description diverges. Runtime says "Perform the placement, asking player guidance when necessary." — the "asking player guidance" caveat is meaningful and must be preserved.
- **module/applications/api/primary-sheet-mixin.d.mts** — `_attachFrameListeners` (fabricated): _attachFrameListeners is defined on ApplicationV2Mixin (application-v2-mixin.mjs), not on PrimarySheetMixin. It should not be declared as a member of BasePrimarySheet5e; it is inherited from a different mixin.
- **module/applications/api/primary-sheet-mixin.d.mts** — `_renderModeToggle` (fabricated): _renderModeToggle is defined on ApplicationV2Mixin (application-v2-mixin.mjs), not on PrimarySheetMixin. It is only called from _onRender here; declaring it on BasePrimarySheet5e is incorrect — it belongs to a different mixin's class.
- **module/applications/item/item-directory.d.mts** — `_allowedDropBehaviors` (missing): The runtime defines _allowedDropBehaviors as an @override but it is entirely absent from our declaration. It should be declared as `protected _allowedDropBehaviors(event: DragEvent, data: object): Set<string>`.
- **module/applications/item/item-directory.d.mts** — `_defaultDropBehavior` (missing): The runtime defines _defaultDropBehavior as an @override but it is entirely absent from our declaration. It should be declared as `protected _defaultDropBehavior(event: DragEvent, data: object): string`.
- **module/applications/activity/_types.d.mts** — `ActivityChoiceDialogContext.img` (fabricated): Our file declares `img: string` but the runtime typedef has `icon: { src: string; svg: boolean }`. The field name and shape are both wrong — `img` does not exist in the runtime; it should be `icon` with nested `src` and `svg` sub-properties.
- **module/data/actor/templates/common.d.mts** — `prepareAbilities` (missing): Runtime has a full JSDoc block describing the method and its options (rollData, originalSaves). Our declaration has no doc comment at all. Port the runtime JSDoc.
- **module/data/actor/templates/common.d.mts** — `calculateAbilityCheckProficiency` (missing): Runtime has a full JSDoc block with param/returns annotations. Our declaration has no doc comment. Port the runtime JSDoc.
- **module/data/actor/templates/common.d.mts** — `calculateToolProficiency` (missing): Runtime has a full JSDoc block with param/returns annotations. Our declaration has no doc comment. Port the runtime JSDoc.
- **module/data/actor/templates/common.d.mts** — `calculateSkillToolProficiency` (missing): Runtime has a full JSDoc block with param/returns annotations. Our declaration has no doc comment. Port the runtime JSDoc.
- **module/data/actor/templates/_fields.d.mts** — `DetailsField.CreatureSchema (level field)` (missing): The runtime details.mjs `creature` getter includes a `level` field (persisted:false) that is entirely absent from our `DetailsCreatureSchema`. It must be added: `level: foundry.data.fields.NumberField<{ integer: true; min: 0; initial: 0 }>; // persisted:false`
- **module/data/actor/templates/creature.d.mts** — `prepareSkill` (altered): Our doc is a single-line summary that drops all 8 @param descriptions, including the important 'If undefined, ...' fallback semantics for each option. This is a meaningful information loss, not just trimming.
- **module/data/item/templates/starting-equipment.d.mts** — `EquipmentEntryData.generateLabel` (missing): Runtime method has full JSDoc including description and all @param entries; our declaration has none.
- **module/data/item/templates/starting-equipment.d.mts** — `EquipmentEntryData.prefixOrEntries` (missing): Runtime static method has full JSDoc with description and all @param entries; our declaration has none.
- **module/data/item/templates/identifiable.d.mts** — `Methods.preUpdateIdentifiable` (missing): Runtime has a full JSDoc block including param descriptions, @returns, @see, and @protected. Our Methods interface has no doc at all. Port the full runtime comment including param/returns/see tags.
- **module/applications/journal/_types.d.mts** — `JournalEditorConfiguration` (missing): The entire typedef is absent from our file. It should be declared as an interface (or equivalent) with a `textKeyPath: string` property carrying the runtime doc comment "The path to the specific HTML field being edited.".

## Spurious comments to remove (36)

- **module/drag-drop.d.mts** :1 — `/** Extension of core's DragDrop providing system drag payload/dropEffect tracki` — This is a file-level header that merely restates the class doc in shorter form with no additional information. The class declaration immediately below already carries the proper doc comment, making this header redundant noise.
- **documents/activity/mixin.d.mts** :74 — `// ---- Protected activation helpers ----` — Section divider comment that merely restates the grouping already visible from method names and proximity; adds no information beyond what the runtime's /* --- */ separators convey, and is pure conversion scaffolding.
- **documents/activity/mixin.d.mts** :141 — `// ---- Event Listeners ----` — Same pattern — section divider replicating runtime separator blocks. Adds no information.
- **documents/activity/mixin.d.mts** :155 — `// ---- Helpers ----` — Same pattern — section divider replicating runtime separator blocks.
- **documents/activity/mixin.d.mts** :183 — `// ---- Statics ----` — Same pattern — section divider replicating runtime separator blocks.
- **documents/activity/mixin.d.mts** :26 — `// ---- Properties ----` — Same pattern — section divider replicating runtime separator blocks.
- **documents/activity/mixin.d.mts** :53 — `// ---- Activation ----` — Same pattern — section divider replicating runtime separator blocks.
- **documents/activity/mixin.d.mts** :168 — `* Note: returns `any` because the concrete Activity type is not expressible from` — This is a legitimate type-design rationale comment and should be KEPT — do not remove.
- **documents/activity/mixin.d.mts** :173 — `* Note: ActivityRollData is not yet defined in gen defs; return type stays loose` — This is a legitimate type-design rationale comment explaining why the return type is `object` rather than a precise type — should be KEPT.
- **documents/advancement/mixin.d.mts** :23 — `// ---- Instance Properties ----` — Section divider comment with no informational value beyond what the code structure already shows; pure visual noise added during conversion.
- **documents/advancement/mixin.d.mts** :35 — `// ---- Preparation Methods ----` — Section divider comment with no informational value; conversion noise.
- **documents/advancement/mixin.d.mts** :39 — `// ---- Display Methods ----` — Section divider comment with no informational value; conversion noise.
- **documents/advancement/mixin.d.mts** :52 — `// ---- Application Methods ----` — Section divider comment with no informational value; conversion noise.
- **documents/advancement/mixin.d.mts** :64 — `// ---- Event Handlers ----` — Section divider comment with no informational value; conversion noise.
- **documents/advancement/mixin.d.mts** :68 — `// ---- Static Members ----` — Section divider comment with no informational value; conversion noise.
- **documents/mixins/flags.d.mts** :10 — `/** @privateRemarks All mixin classes accept anything for their constructor. */` — Restates the structural necessity of `...args: any[]` which is obvious from the declaration and the file-header explanation; adds no dnd5e-domain information.
- **module/config/traits.d.mts** :60 — `// field/Number constructor or boolean flag` — Inline comment restates what the JSDoc directly above already says ('Type of data represented (a field/Number constructor or boolean flag)'), adding zero new information.
- **module/dice/_types.d.mts** :229 — `// TODO: typeof RollConfigurationDialog once ported` — Scaffolding TODO left from conversion tracking work — this is a process artifact noting a pending port, not a genuine design rationale comment.
- **module/data/fields/identifier-field.d.mts** :26 — `/** Module-scoped value class so `IdentifierField` is usable as a value (constru` — This is a conversion-scaffolding note explaining the dual namespace+class pattern used across the package, not a description of IdentifierField's runtime behavior. It restates a structural pattern rather than adding field-specific information.
- **module/data/fields/activities-field.d.mts** :31 — `Runtime: `activities-field.mjs` line 28, `export class ActivityField extends fou` — Conversion artifact — references a source-file line number. Adds no semantic value to the declaration; the class identity is already clear from the TypeScript declaration itself.
- **module/data/fields/activities-field.d.mts** :48 — `Runtime: `activities-field.mjs` line 81, `export class ActivityCollection extend` — Same pattern — line-number reference from conversion process. The class identity is expressed by the TypeScript declaration; this comment is noise.
- **module/data/advancement/scale-value-data.d.mts** :97 — `Runtime: `ScaleValueType` (scale-value-data.mjs line 80).` — Merely restates the file and line number of the runtime counterpart — no semantic value beyond what the file structure already implies. Conversion-navigation artifact.
- **module/data/advancement/scale-value-data.d.mts** :149 — `Runtime: `ScaleValueTypeNumber` (scale-value-data.mjs line 223).` — Same pattern — restates file/line of the runtime counterpart with no semantic content.
- **module/data/advancement/scale-value-data.d.mts** :157 — `Runtime: `ScaleValueTypeCR` (scale-value-data.mjs line 270).` — Same pattern — restates file/line of the runtime counterpart with no semantic content.
- **module/data/advancement/scale-value-data.d.mts** :166 — `Runtime: `ScaleValueTypeDice` (scale-value-data.mjs line 320).` — Same pattern — restates file/line of the runtime counterpart with no semantic content.
- **module/data/advancement/scale-value-data.d.mts** :192 — `Runtime: `ScaleValueTypeDistance` (scale-value-data.mjs line 453).` — Same pattern — restates file/line of the runtime counterpart with no semantic content.
- **module/data/advancement/scale-value-data.d.mts** :201 — `Runtime: `ScaleValueTypeUsage` (scale-value-data.mjs line 483).` — Same pattern — restates file/line of the runtime counterpart with no semantic content.
- **module/applications/advancement/advancement-config.d.mts** :52 — `/** @internal appv1 form submission hook. */` — The runtime uses @inheritDoc for _updateObject. This comment was added during conversion and is not from the runtime; it restates that this is an appv1 hook, which is already conveyed by the class heritage and @inheritDoc pattern used on all other overrides in the file.
- **module/applications/item/item-sheet.d.mts** :92 — `// _prepareContext` — This comment merely labels a section by method name inside the RenderContext interface without adding any information not already evident from the surrounding field names. It is an organizational label that restates the method name and adds no semantic value.
- **module/applications/activity/transform-sheet.d.mts** :18 — `/** The transform-profile SchemaField element type from the transform activity s` — This restates the TypeScript type expression (ProfileElement = Schema["profiles"]["...__fvtt..."]). It adds no information beyond the member name and type already visible in the declaration.
- **module/applications/activity/transform-sheet.d.mts** :20 — `/** The DataField record for the transform-profile element schema. */` — This restates 'ProfileFields = ProfileElement["fields"]' — the type itself already says it is the fields record of the element. No runtime doc exists for this helper type; the comment just paraphrases the assignment.
- **module/data/item/templates/mountable.d.mts** :38 — `/** Instance methods this template MIXES onto a model (composed onto items via a` — This comment describes the TypeScript pattern used for mixins (companion interface), not runtime behavior. While informative, it restates what the interface declaration itself conveys; it is types-infrastructure rationale. However, since it describes design intent and not conversion noise, it is borderline — keeping it is defensible. Flagging as low-confidence noise only.
- **module/data/item/templates/item-type.d.mts** :14 — `/** Instance members this template MIXES onto a model (composed onto items via a` — This comment describes the TypeScript declaration pattern (companion interface / mixin composition) rather than any runtime behavior. While not pure noise, it describes the types infrastructure rather than the dnd5e domain — borderline; keeping it is also defensible as architecture rationale.
- **module/data/item/templates/starting-equipment.d.mts** :36 — `/** Instance methods this template MIXES onto a model (composed onto items via a` — This is a types-infrastructure design note explaining the mixin pattern; it is not runtime noise. KEEP — do not remove.
- **module/data/item/templates/identifiable.d.mts** :21 — `/** Instance methods this template MIXES onto a model (composed onto items via a` — This comment merely restates what the interface name and pattern already communicate — that Methods holds instance methods mixed onto a model. It adds no runtime-sourced or type-design rationale information.
- **module/data/item/fields/item-type-field.d.mts** :24 — `/** Module-scoped value class so `ItemTypeField` is usable as a value/constructo` — This is a conversion-infrastructure note explaining why the class exists as a value declaration. It is a legitimate design rationale comment, not noise.

## Top 25 files by finding count

- 26 — documents/advancement/mixin.d.mts
- 20 — module/data/actor/templates/_fields.d.mts
- 15 — module/data/item/templates/starting-equipment.d.mts
- 14 — documents/activity/mixin.d.mts
- 14 — module/data/actor/_types.d.mts
- 14 — module/applications/api/primary-sheet-mixin.d.mts
- 13 — module/utils.d.mts
- 13 — module/data/advancement/item-choice-data.d.mts
- 12 — module/data/abstract/system-data-model.d.mts
- 11 — module/data/advancement/ability-score-improvement-data.d.mts
- 11 — module/data/item/templates/physical-item.d.mts
- 10 — module/dice/basic-roll.d.mts
- 10 — module/applications/actor/character-sheet.d.mts
- 9 — module/enrichers.d.mts
- 9 — module/config/traits.d.mts
- 9 — module/canvas/ability-template.d.mts
- 9 — module/data/activity/base-activity.d.mts
- 9 — module/data/activity/save-data.d.mts
- 9 — module/data/actor/templates/_types.d.mts
- 8 — documents/actor.d.mts
- 8 — module/applications/actor/group-sheet.d.mts
- 8 — module/data/activity/fields/consumption-targets-field.d.mts
- 7 — module/data/actor/npc.d.mts
- 7 — module/data/advancement/trait-data.d.mts
- 7 — module/applications/advancement/advancement-manager.d.mts
