{"documents/active-effect.d.mts":[{"severity":"low","axis":"fidelity","member":"applyActivity / _applyChangeShim / _prepareFlagChange / applyChange / applyChangeField","fix":"Type change params as the foundry EffectChangeData shape (ActiveEffect.ChangeData) instead of `object` where reachable.","note":"Confirmed: runtime JSDoc uses {EffectChangeData} for these change params/returns (active-effect.mjs:249,340,341,382,383). The reachable named type exists in fvtt-types as ActiveEffect.ChangeData (re-exported at node_modules/fvtt-types/.../common/documents/active-effect.d.mts:327). Bare `object` loses the key/value/mode/priority change shape. Real fidelity gap; low severity is fair since these are mostly internal apply-machinery params."}],"documents/activity/cast.d.mts":[{"severity":"low","axis":"coverage","member":"getCachedSpellData","fix":"Add `getCachedSpellData(): Promise<object | void>;`","note":"Confirmed: cast.mjs:118 async getCachedSpellData() returns Promise<object|void> per JSDoc. Absent. Low is appropriate (helper method, object return acceptable since runtime produces a cloned source object with no reachable named type)."},{"severity":"low","axis":"coverage","member":"getSpellChanges","fix":"Add `getSpellChanges(): object[];`","note":"Confirmed: cast.mjs:149 getSpellChanges() returns object[] (EffectChangeData array) per JSDoc. Absent. Low appropriate."},{"severity":"low","axis":"coverage","member":"getRollData","fix":"Add `getRollData(...options: any[]): object;` matching the base mixin getRollData signature.","note":"Confirmed: cast.mjs:191-196 overrides getRollData(...options). Neither cast.d.mts nor mixin.d.mts declares it. Genuine coverage gap. Low appropriate; fix ideally mirrors the actual base signature rather than any[], but adding the override is valid."}],"documents/activity/mixin.d.mts":[{"severity":"low","axis":"fidelity","member":"consume / refund / _mergeActivityUpdates","fix":"On consume, replace object/unknown with ActivityUseConfiguration, ActivityMessageConfiguration, and return ActivityUsageUpdates|false. For refund and _mergeActivityUpdates, type their params with ActorDeltasData / ActivityUsageUpdates when those members are added.","note":"Real but largely overlaps finding #3 (consume correctness). Named defs ActivityUseConfiguration/ActivityMessageConfiguration/ActivityUsageUpdates exist in activity gen defs; ActorDeltasData exists in src/_gen/data/chatMessage/fields/_types.d.mts (not under activity). refund and _mergeActivityUpdates are not currently declared at all, so their fidelity is subsumed by the coverage fix. Downgraded to low to avoid double-counting consume."},{"severity":"low","axis":"shortcut","member":"prepareData / prepareFinalData / isSpell / ability / actionType / relevantLevel","fix":"Remove these data-model-owned members from ActivityBehavior; they reach the mixed type via FixedInstanceType<T> in ActivityMix. Reserve ActivityBehavior for the Activity-class-owned (mixin-owned) surface.","note":"Confirmed owned by the data model in base-activity.mjs:105 (ability),115 (actionType),200 (isSpell),211 (relevantLevel),560 (prepareData),577 (prepareFinalData) — not the mixin's Activity class. ActivityMix intersects FixedInstanceType<T> & ActivityBehavior, so these arrive via T; re-declaring on ActivityBehavior is duplication and a drift risk. Genuine but low severity."}],"documents/activity/save.d.mts":[{"severity":"low","axis":"coverage","member":"getFavoriteData","fix":"Add 'getFavoriteData(): Promise<...>' to SaveActivity reflecting the merged { save } shape (or rely on base if base declares it).","note":"low — unverified"},{"severity":"low","axis":"coverage","member":"static LOCALIZATION_PREFIXES","fix":"Add 'static LOCALIZATION_PREFIXES: readonly string[];' if per-class static surfacing is desired.","note":"low — unverified"},{"severity":"low","axis":"coverage","member":"_usageChatButtons","fix":"Add '_usageChatButtons(message: ChatMessage5e): object[];' if protected overrides are surfaced.","note":"low — unverified"}],"documents/actor.d.mts":[{"severity":"low","axis":"correctness","member":"spellcastingAbility","fix":"Change return type to `dnd5e.types.Ability.TypeKey`.","note":"Confirmed runtime line 142 ends `?.ability ?? this.system.attributes?.spellcasting ?? \"int\"`, so the value always resolves to an ability key; the `| \"\"` union member is unreachable. Genuine but minor over-wide type; low severity correct."}],"documents/actor/hit-dice.d.mts":[{"severity":"low","axis":"fidelity","member":"smallestAvailable / largestAvailable","fix":"Type as `get smallestAvailable(): `d${number}`;` (and likewise largestAvailable); this covers both the `d${faces}` denomination keys and the literal \"d0\" fallback.","note":"Confirmed. Returns either `d${faces}` (faces a number from this.sizes) or the literal \"d0\" (hit-dice.mjs:90-96,124-130). Both are denomination-shaped `d${number}` strings, never arbitrary strings. The proposed `HitDieType.TypeKey | \"d0\"` is acceptable but `d${number}` is simpler and fully accurate."}],"documents/actor/proficiency.d.mts":[{"severity":"low","axis":"shortcut","member":"<file>","fix":"Either add a merged `interface Proficiency {}` next to the class to back the documented augmentation seam, or correct the header comment to describe only the namespace-based extension actually provided.","note":"low — unverified"}],"documents/actor/select-choices.d.mts":[{"severity":"low","axis":"correctness","member":"SelectChoices.Entry.label","fix":"Make label required: change `label?: string` to `label: string` in interface Entry.","note":"low — unverified"},{"severity":"low","axis":"shortcut","member":"<file>","fix":"Comment-document the TS limitation, or model entries via a typed accessor rather than `Entry | unknown`.","note":"low — unverified"},{"severity":"low","axis":"fidelity","member":"SelectChoices.OptionEntry","fix":"Type asOptions as `foundry.applications.fields.FormSelectOption[]` (or extend it) instead of the bespoke OptionEntry.","note":"low — unverified"}],"documents/advancement/item-grant.d.mts":[{"severity":"low","axis":"coverage","member":"apply","fix":"Add an item-grant `apply` override: `apply(level: number, data?: { ability?: dnd5e.types.Ability.TypeKey; retainedData?: object; selected?: string[] }, options?: object): Promise<object>;`. The base AdvancementBehavior.apply returns Promise<void>, which is wrong for this subclass.","note":"Confirmed: runtime item-grant.mjs:91 overrides apply with the options object `{ ability, retainedData, selected }` plus a third `options` param and returns the `updates` object (line 135). The mixin base apply is `apply(level, data): Promise<void>` (mixin.d.mts:25), so both the richer signature and the non-void return are missing. The minimal per-advancement decl convention (cf. item-choice.d.mts) does not cover this item-grant-specific override. Low severity is appropriate."}],"documents/advancement/trait.d.mts":[{"severity":"low","axis":"correctness","member":"metadata","fix":"If used purely as a type-level discriminant keep as-is; otherwise align with runtime by keying the discriminant off the registration/config name rather than metadata.name.","note":"low — unverified"}],"documents/chat-message.d.mts":[{"severity":"low","axis":"correctness","member":"_trayStates","fix":"Type it `_trayStates: Map<string, boolean> | undefined;` (or mark optional).","note":"Confirmed: runtime declares `_trayStates;` with no initializer (chat-message.mjs:76, JSDoc @type {Map<string, boolean>}) and every use guards with `this._trayStates?.get` (lines 163,166). The .d.mts types it non-optional `Map<string, boolean>`, hiding the genuinely-possible undefined state. Real correctness nit; low is correct."}],"documents/combatant.d.mts":[{"severity":"low","axis":"fidelity","member":"CombatRecoveryResults.rolls","fix":"Change `rolls: unknown[]` to `rolls: import(\"../module/dice/basic-roll.mjs\").default[];`.","note":"Runtime pushes BasicRoll instances into results.rolls (combatant.mjs:134). BasicRoll is ported. Same path correction as above: use the default-import path, not `dnd5e.types.Dice.BasicRoll.Any[]`. The sibling actor/item fields remaining `object`/`object[]` is correct (genuinely untyped update shapes)."}],"documents/item.d.mts":[{"severity":"low","axis":"correctness","member":"scaledClone","fix":"Change first param to `scaling: number`.","note":"Confirmed: runtime signature item.mjs:537 is `scaledClone(scaling, options={})` with `@param {number} scaling` (533). The `.d.mts` `number | object` admits an unsupported shape. Real but minor; reclassified coverage->correctness."},{"severity":"low","axis":"correctness","member":"SpellcastingDescription.levels","fix":"Make `levels: number | null` and `progression: dnd5e.types.Spellcasting.Progression.TypeKey | null`.","note":"Confirmed against typedef _types.mjs:204-209: `progression: string|null`, `levels: number|null`. The .d.mts types both as non-null, dropping the null cases. Real correctness gap, low severity."},{"severity":"low","axis":"fidelity","member":"compendiumBrowserTypes","fix":"Type return as the SelectChoices instance type from src/documents/actor/select-choices.d.mts.","note":"Confirmed: runtime returns `new SelectChoices(choices)` (item.mjs:86); the type exists in-repo at src/documents/actor/select-choices.d.mts. The `.d.mts` `unknown` is a reachable-named-type fidelity miss."},{"severity":"low","axis":"correctness","member":"createWithContents","fix":"Type params as `(globalThis.Item.Implementation | object)[]` and return `Promise<object[] | undefined>` (returns prepared creation DATA, not item instances; can return undefined on MAX_DEPTH).","note":"Confirmed: runtime (item.mjs:1297-1329) accepts Item5e[] or data, returns the `created` array of plain creation-data objects (`Promise<object[]>` per @returns), and returns undefined early on MAX_DEPTH (1303). The `.d.mts` return `Promise<Item.Implementation[]>` is wrong (returns data, not instances). The param looseness is minor but the return type is a genuine correctness error."},{"severity":"low","axis":"correctness","member":"createScrollFromSpell","fix":"Add third param `config?: object` and change return to `Promise<globalThis.Item.Implementation | void>`.","note":"Confirmed: runtime item.mjs:1340 is `static async createScrollFromSpell(spell, options={}, config={})` with SpellScrollConfiguration third arg; returns `new this(spellScrollData)` or void (Promise<Item5e|void>). The .d.mts omits the config param and adds a spurious `| object` to the return. Real correctness gap."}],"documents/mixins/document.d.mts":[{"severity":"low","axis":"correctness","member":"SystemDocumentMixin","fix":"Import DependentDocumentMixin and change the inner type to ReturnType<typeof DependentDocumentMixin<ReturnType<typeof SystemFlagsMixin<T>>>>.","note":"Confirmed against runtime: document.mjs:12 composes `class SystemDocument extends DependentDocumentMixin(SystemFlagsMixin(Base))`, but document.d.mts:21-23 only wraps SystemFlagsMixin<T> and never imports DependentDocumentMixin. dependent.d.mts exists and is reachable, so the heritage chain is genuinely incomplete in the type. Severity downgraded medium->low: DependentDocumentMixin (dependent.mjs / dependent.d.mts) introduces NO new public surface — it only overrides prepareData/_onDelete (its own doc comment states it is 'transparent at the type level'). So no member is actually lost from the resulting Actor5e_base/Item5e_base type; only the structural fidelity of the prototype chain is off. Fix is accurate; for 1:1 heritage fidelity it should layer the dependent mixin."}],"documents/token.d.mts":[{"severity":"low","axis":"shortcut","member":"<file>","fix":"Correct the docblock (lines 9-13) to describe the class+namespace merge as the actual seam, or add `declare interface TokenDocument5e {}` as an open downstream merge target.","note":"Confirmed: docblock lines 9-10 claim the class is merged with a same-name interface TokenDocument5e for downstream augmentation, but only a class + namespace are declared; no merge-target interface exists. The narrative is misleading. Low severity is appropriate; it is a documentation/seam-description inaccuracy rather than a compile error."}],"documents/user.d.mts":[{"severity":"low","axis":"seam","member":"interface User5e","fix":"Add an empty 'interface User5e {}' alongside the class to provide the documented declaration-merge seam for downstream document-level methods/getters.","note":"low — unverified"}],"funnel/flag-config.d.mts":[{"severity":"low","axis":"fidelity","member":"FlagConfig.ChatMessage.roll","fix":"Type roll as { type?: \"attack\"|\"damage\"|\"healing\"|\"death\"|...; ammunition?; ammunitionData?; attackMode?; damageOnSave?; ... } instead of bare object.","note":"Confirmed: roll.type compared against string-literal domain \"attack\",\"damage\",\"death\" (chat-message.mjs:240-241,344-349) and \"healing\" (actor.mjs:797). A real key-union exists per the strictness mandate; bare object is a fidelity shortcut."},{"severity":"low","axis":"fidelity","member":"FlagConfig.ActiveEffect.riders","fix":"Type riders as { effect?: string[]; statuses?: string[]; activity?: string } instead of bare object.","note":"Confirmed concrete shape: riders.effect is string[] (base.mjs:51, .includes(...)), riders.statuses read, riders.activity read via getFlag. Bare object is a fidelity shortcut where a named shape demonstrably exists."}],"funnel/system-config.d.mts":[{"severity":"low","axis":"shortcut","member":"ActiveEffect","fix":"Correct the comment to: dnd5e registers only `enchantment`; keep `base` because untyped effects (type \"base\") still occur and must remain in the union.","note":"Confirmed: system.json documentTypes.ActiveEffect contains only { enchantment: {} }. `base` is Foundry's untyped fallback subtype, not a dnd5e-registered subtype. The comment at lines 45-46 (\"dnd5e's ActiveEffect subtypes are literally `base` and `enchantment` ... the foundry 'base' fallback IS a real, registered dnd5e subtype here\") is factually wrong and would mislead a maintainer. The actual decision to omit `base:\"ignore\"` (keeping `base` in the union) is fine and correct; only the stated rationale is inaccurate. Low severity: comment-only, no type impact."}],"module/data/abstract/active-effect-data-model.d.mts":[{"severity":"low","axis":"fidelity","member":"onRenderActiveEffectConfig","fix":"onRenderActiveEffectConfig(app: foundry.applications.sheets.ActiveEffectConfig.Implementation, html: HTMLElement, context: foundry.applications.sheets.ActiveEffectConfig.RenderContext): void;","note":"low — unverified"}],"module/data/activity/attack-data.d.mts":[{"severity":"low","axis":"coverage","member":"getActionLabel","fix":"Add `getActionLabel(attackMode?: string): string;` to the class body.","note":"Confirmed: runtime attack-data.mjs:227 defines public method getActionLabel(attackMode?): string; not on base. Absent from the .d.mts. Low is fair (single optional string param, label helper)."},{"severity":"low","axis":"fidelity","member":"getAttackData","fix":"When adding getAttackData, type the return as `{ data: object; parts: string[] }` — keep parts as string[], do not loosen to string or any.","note":"Contingent on the getAttackData coverage fix. Runtime returns CONFIG.Dice.BasicRoll.constructParts(...) which yields `{ data, parts }` where parts is a string array (joined with '+' at line 212). `data` has no reachable named type so `object` is acceptable; only parts must stay string[]. Low severity."}],"module/data/activity/base-activity.d.mts":[{"severity":"low","axis":"coverage","member":"INITIAL_ID / LOCALIZATION_PREFIXES","fix":"Add `static INITIAL_ID: string;` and `static override LOCALIZATION_PREFIXES: string[];` to BaseActivityData.","note":"Confirmed present on runtime (INITIAL_ID mjs:258, LOCALIZATION_PREFIXES mjs:38) and absent from the .d.mts. LOCALIZATION_PREFIXES has clear precedent in sibling bases (system-data-model.d.mts, vehicle.d.mts, item/templates/activities.d.mts). Real but minor static config; downgraded from medium to low."},{"severity":"low","axis":"fidelity","member":"Metadata.usage.dialog","fix":"Type dialog as the usage-dialog constructor, e.g. `dialog: typeof dnd5e.types... ActivityUsageDialog` or `fvttUtils.AnyConstructor`, instead of bare `object`.","note":"Confirmed. Metadata.usage.dialog typed bare `object` (base-activity.d.mts:71) but runtime mixin metadata sets `dialog: ActivityUsageDialog` (mixin.mjs:43) and uses it as applicationClass (mixin.mjs:197). A concrete ActivityUsageDialog type exists at src/module/applications/activity/activity-usage-dialog.d.mts. Per the strictness mandate this is a reachable fidelity improvement. (Sibling `actions: Record<string,unknown>` is genuinely dynamic — correctly left loose.)"},{"severity":"low","axis":"fidelity","member":"ActivityBehavior.prepareFinalData (rollData param)","fix":"In mixin.d.mts type rollData as `dnd5e.types.defs.documents.ActivityRollData` instead of bare `object`.","note":"Confirmed genuine but the member lives in mixin.d.mts:34, NOT base-activity.d.mts (base file declares no prepareFinalData). Runtime prepareFinalData(rollData) takes an ActivityRollData (mjs:575-577) and the named type is reachable at src/_gen/documents/_types.d.mts:109 (dnd5e.types.defs.documents.ActivityRollData). Real fidelity shortcut; low severity since it is the mixin override, not the in-scope base file."}],"module/data/activity/damage-data.d.mts":[{"severity":"low","axis":"coverage","member":"getDamageConfig","fix":"Add `getDamageConfig(config?: object, options?: object): object;` to BaseDamageActivityData (or model it on the BaseActivityData base) matching the runtime override.","note":"low — unverified"},{"severity":"low","axis":"coverage","member":"transformTypeData","fix":"Add `static override transformTypeData(source: object, activityData: object, options: object): object;` to BaseDamageActivityData mirroring the base-activity signature.","note":"low — unverified"}],"module/data/activity/_registry.d.mts":[{"severity":"low","axis":"coverage","member":"Collection.every","fix":"Add `every(predicate: (value: dnd5e.types.Activity.Instance, index: number, collection: this) => boolean): boolean;` to interface Collection.","note":"low — unverified"}],"module/data/activity/save-data.d.mts":[{"severity":"low","axis":"fidelity","member":"Save.Schema.damage.onSave","fix":"Add the value union: StringField<{ required:true; blank:false; initial:\"half\" }, \"none\"|\"half\"|\"full\">.","note":"Confirmed: sheet onSaveOptions (save-sheet.mjs:58-61) is exactly \"none\"|\"half\"|\"full\"; transformTypeData (line 78) and _preCreate (line 132) set \"none\"/\"half\". The literal union is demonstrable but lives only in an inline sheet array with no named CONFIG type, so low severity is appropriate."}],"module/data/activity/transform-data.d.mts":[{"severity":"low","axis":"correctness","member":"transform.mode","fix":"Type as `StringField<{ initial: \"cr\" }, \"\" | \"cr\">` (or RestrictedStringField) to pin the literal union.","note":"Confirmed: _types.mjs:202 documents transform.mode as \"\"|\"cr\"; schema (line 53) types it as bare StringField<{initial:\"cr\"}> with no value union, losing the constraint. Matches the project strictness mandate (domain values get real key unions). Real; low severity is appropriate for a narrow value-union loss."}],"module/data/activity/utility-data.d.mts":[{"severity":"low","axis":"coverage","member":"transformTypeData","fix":"Add `static override transformTypeData(source: object, activityData: object, options?: object): object;` to the declared class (and ideally to the base).","note":"low — unverified"}],"module/data/actor/npc.d.mts":[{"severity":"low","axis":"coverage","member":"_prepareEmbedContext","fix":"Add `_prepareEmbedContext(rulesVersion: \"2014\" | \"2024\"): Promise<object>;` to the NPCData class.","note":"Confirmed: runtime npc.mjs:607 defines `async _prepareEmbedContext(rulesVersion)`, an instance method absent from the .d.mts. It is an underscore-prefixed internal helper used only by toEmbed; siblings do not declare their `_`-helpers, so value is marginal. Genuine method but low priority."},{"severity":"low","axis":"correctness","member":"cantripLevel","fix":"Change signature to `cantripLevel(spell: globalThis.Item.Implementation): number | null;`.","note":"Confirmed: runtime npc.mjs:502 returns `this.details.cr` for innate spells, and DetailsSchema.cr is `nullable: true` (npc.d.mts line 73). So the derived return can be `number | null` but is typed `number`. Real but minor nullability narrowing miss; low severity appropriate."}],"module/data/actor/templates/creature.d.mts":[{"severity":"low","axis":"coverage","member":"_spellLevels","fix":"Add `static get _spellLevels(): dnd5e.types.Actor.Creature.SpellLevelKey[];` to the class body.","note":"Confirmed at creature.mjs:98-101: real `static get _spellLevels()` returning `spell1..9` + `pact`. Absent from the d.mts. It is @private (internal default-keys helper), so the coverage value to consumers is minimal; low is appropriate. Return type maps cleanly to the existing SpellLevelKey union."},{"severity":"low","axis":"coverage","member":"_initialSkillValue","fix":"Add `static _initialSkillValue(key: string, initial: object): object;` to the class body.","note":"Confirmed at creature.mjs:86-89: real `static _initialSkillValue(key, initial)`. Absent from the d.mts. It is @private (MappingField initialValue helper), so low severity is correct. The runtime takes/returns the dynamic initial skill object, so object is acceptable precision."}],"module/data/actor/templates/_fields.d.mts":[{"severity":"low","axis":"coverage","member":"AttributesFields._migrateArmorClass/_migrateInitiative","fix":"Add `/** @internal */ static _migrateArmorClass(source?: object): void;` and `/** @internal */ static _migrateInitiative(source?: object): void;`.","note":"Confirmed: attributes.mjs _migrateArmorClass (:146) and _migrateInitiative (:182) are real statics, marked @internal. Low because internal migration helpers are rarely called by consumers, but they are genuine missing members."}],"module/data/actor/vehicle.d.mts":[{"severity":"low","axis":"fidelity","member":"canConsumeCrewAction","fix":"Change signature to `static canConsumeCrewAction(activity: dnd5e.types.Activity.Instance): boolean | void;`","note":"Confirmed. Line 298 types param as `activity: object`; runtime vehicle.mjs:401 JSDoc is `@param {Activity}` and the body reads activity.actor. dnd5e.types.Activity.Instance is a documented known-good convention. Genuine fidelity issue, low severity appropriate."}],"module/data/advancement/item-choice-data.d.mts":[{"severity":"low","axis":"correctness","member":"ItemChoice.ConfigSchema.spell","fix":"Add the initial option: `EmbeddedDataField<typeof import(\"./spell-config.mjs\").default, { nullable: true; initial: null }>`.","note":"Confirmed. Runtime item-choice-data.mjs:48 is `new EmbeddedDataField(SpellConfigurationData, { nullable: true, initial: null })`; the .d.mts:40 omits initial:null. Without it the derived initial/optionality typing does not reflect that the field defaults to null rather than a constructed instance. Genuine correctness omission, correctly rated low."},{"severity":"low","axis":"coverage","member":"ItemChoiceConfigurationData.migrateData","fix":"Add `static override migrateData(source: object): object;` to BaseItemChoiceConfigData.","note":"Confirmed. Runtime ItemChoiceConfigurationData defines `static migrateData(source)` at item-choice-data.mjs:58 (converts numeric choices to {count}, string pool entries to {uuid}, delegates spell migration). The .d.mts BaseItemChoiceConfigData declares only defineSchema, omitting the migrateData static override. Real coverage gap, low severity appropriate."}],"module/data/advancement/item-grant-data.d.mts":[{"severity":"low","axis":"correctness","member":"ItemGrant.ConfigSchema.items","fix":"Add the second type arg: ArrayField<SchemaField<{...}>, { required: true }>.","note":"Confirmed real. item-grant-data.mjs:32 constructs the items ArrayField with { required: true }; the type declares it without options. Minor but genuine drift."},{"severity":"low","axis":"shortcut","member":"ItemGrant.SpellConfigSchema","fix":"Remove the unused SpellConfigSchema alias, or wire it into the spell EmbeddedDataField type.","note":"Confirmed real but minor. The SpellConfigSchema alias (line 17) is declared and never referenced; the spell field (line 30) uses EmbeddedDataField<typeof import(\"./spell-config.mjs\").default> directly. Dead alias / maintenance trap, no runtime drift. Low severity is appropriate."}],"module/data/advancement/scale-value-data.d.mts":[{"severity":"low","axis":"coverage","member":"BaseScaleValueConfigData.migrateData","fix":"Add `static override migrateData(source: object): object;` to BaseScaleValueConfigData (and consider LOCALIZATION_PREFIXES).","note":"Confirmed: ScaleValueConfigurationData overrides `static migrateData(source)` at lines 46-57 (remaps legacy 'numeric'->'number', runs per-entry TYPES migration). The .d.mts declares only defineSchema. Genuine but minor (migrateData rarely consumed by typed callers); low is correct."}],"module/data/advancement/spell-config.d.mts":[{"severity":"low","axis":"coverage","member":"migrateData","fix":"Add `static override migrateData(source: object): object;` to the class.","note":"Confirmed: runtime spell-config.mjs:46 overrides static migrateData(source) (preparation->method/prepared migration, ability string->array). Absent from the .d.mts. Low severity appropriate for a migration override that is rarely called directly by consumers."}],"module/data/calendar/calendar-of-harptos.d.mts":[{"severity":"low","axis":"correctness","member":"CALENDAR_OF_HARPTOS","fix":"Make festivals required: `festivals: dnd5e.types.defs.data.calendar.CalendarConfigHarptosFestival[]`.","note":"low — unverified"}],"module/data/chat-message/fields/deltas-field.d.mts":[{"severity":"low","axis":"fidelity","member":"ActorDeltasField.getDeltas","fix":"Change `updates: object` to `updates: dnd5e.types.defs.data.chatMessage.fields.ActorUpdatesDescription`","note":"Confirmed: ActorDeltasField.getDeltas runtime JSDoc (deltas-field.mjs:29) annotates `@param {ActorUpdatesDescription} updates`, a real named type defined at _gen/data/chatMessage/fields/_types.d.mts:17 reachable as `dnd5e.types.defs.data.chatMessage.fields.ActorUpdatesDescription`. The .d.mts (line 39) types it bare `object`, violating the strictness mandate. Note: this applies only to ActorDeltasField.getDeltas; IndividualDeltaField.getDeltas (line 20) correctly uses `object` since its runtime JSDoc is genuinely `@param {object} updates`."}],"module/data/chat-message/rest-message-data.d.mts":[{"severity":"low","axis":"shortcut","member":"<file>","fix":"Delete the 'Unported custom fields ... typed loosely as ObjectField' sentence from the header docblock since both fields are ported and used with their real types.","note":"low — unverified"}],"module/data/chat-message/time-passed-message-data.d.mts":[{"severity":"low","axis":"shortcut","member":"<file>","fix":"Delete the stale 'Unported custom field ... typed loosely as ObjectField' paragraph from the header comment.","note":"low — unverified"}],"module/data/chat-message/turn-message-data.d.mts":[{"severity":"low","axis":"shortcut","member":"<file>","fix":"Delete the stale 'Unported custom fields (ActivationsField, ActorDeltasField)...typed loosely as ObjectField until those fields are ported' paragraph (lines 9-10) from the header.","note":"Confirmed stale. Both fields/activations-field.d.mts and fields/deltas-field.d.mts exist and are ported. BaseSchema (lines 51,53) correctly uses the real ActivationsField and ActorDeltasField types imported on lines 14-15, not ObjectField. The header comment contradicts the actual code and misleads. Low is appropriate (doc-comment only, no type impact)."}],"module/data/chat-message/usage-message-data.d.mts":[{"severity":"low","axis":"correctness","member":"BaseSchema.effects","fix":"Type inner as `foundry.data.fields.StringField<{ blank: false }>`.","note":"Confirmed: runtime line 27 is `new ArrayField(new StringField({ blank: false }))`; the declared inner StringField drops `blank: false`. Note `blank` is a validation-only option and does not change the resolved `string` type, so impact is minor; low severity is correct."},{"severity":"low","axis":"shortcut","member":"<file>","fix":"Update the header comment to reflect that ActorDeltasField is ported (used here as `ActorDeltasField<{...}>`) and remove the obsolete ObjectField/unported and loose-activity claims.","note":"Confirmed stale: header lines 9-11 claim ActorDeltasField is typed loosely as ObjectField until ported, but deltas-field.d.mts is fully ported and used here generically as `ActorDeltasField<{ required:true; nullable:true; initial:null }>`. The activity getter also returns the known-good `dnd5e.types.Activity.Instance | undefined`, not a loose type, so that clause is misleading too. Documentation-only, low severity."}],"module/data/fields/advancement-collection-field.d.mts":[{"severity":"low","axis":"correctness","member":"AdvancementCollectionField#constructor","fix":"Add `constructor(options?: dnd5e.types.fields.MappingField.DefaultOptions);` to the declared class body so the arity matches the runtime single-arg signature.","note":"Confirmed: runtime constructor (advancement-collection-field.mjs:9) is `constructor(options)` calling `super(new AdvancementField(), options)`. The declared class extends `foundry.data.fields.ObjectField` (deliberately, to reuse registry-derived types) and thus inherits ObjectField's `(options?, context?)` constructor, so `new AdvancementCollectionField(opts, context)` is incorrectly permitted. `MappingField.DefaultOptions` exists (mapping-field.d.mts:31). Minor surface-level arity mismatch; low severity is appropriate."}],"module/data/fields/formula-field.d.mts":[{"severity":"low","axis":"coverage","member":"FormulaField#deterministic","fix":"Add `deterministic: boolean;` to the `declare class FormulaField { ... }` body (and surface it on the alias if instance access is desired).","note":"low — unverified"}],"module/data/fields/identifier-field.d.mts":[{"severity":"low","axis":"seam","member":"dnd5e.types.fields.IdentifierField","fix":"Introduce `namespace IdentifierField { type Options = ...; type DefaultOptions = ... }` alongside the type alias to match the field-pattern seam.","note":"Confirmed. FormulaField exposes an open companion namespace (Options/DefaultOptions) for declaration-merging and option discovery; IdentifierField has none, leaving no seam to extend its options. Same root cause as the correctness finding (missing namespace + extra options), framed as the missing seam. Low severity appropriate since it's the structural facet of the medium correctness gap."}],"module/data/fields/roll-config-field.d.mts":[{"severity":"low","axis":"correctness","member":"RollConfigField.RollSchema.min/max","fix":"Add `min: 1; max: 20` to both the min and max NumberField option objects in RollSchema.","note":"Confirmed. opts = { initial: null, nullable: true, min: 1, max: 20, integer: true } (roll-config-field.mjs:10) is spread into both min and max NumberFields (lines 18-19). The .d.mts RollSchema (lines 29-30) has only { nullable: true; integer: true; initial: null }, omitting min:1 and max:20. Valid NumberField options; low because they are validation constraints, not value-shape changes."}],"module/data/fields/_shims.d.mts":[{"severity":"low","axis":"correctness","member":"SimpleTraitField.Base.value","fix":"Add { initial: [] } (and required:true to match an always-present set) to SimpleTraitField.Base.value (line 33) so its derived optionality mirrors the runtime's always-initialized Set.","note":"Confirmed against simple-trait-field.mjs line 9: SetField is created with { initial: initialValue } where initialValue defaults to []. The shim's Base.value (line 33) is a SetField with no initial, so the initialized type may be optional/possibly-undefined where the runtime guarantees a present Set. Low severity since SetField often initializes to empty by default, but the explicit initial is not mirrored."},{"severity":"low","axis":"coverage","member":"LocalDocumentField","fix":"Add a `declare class LocalDocumentField` value-side counterpart and include it in the export list (lines 151-152), or document why it is alias-only despite the six siblings having value classes.","note":"Confirmed: LocalDocumentField is the only one of the seven shims with a type alias (lines 83-89) but no `declare class` value counterpart and no entry in the export lists (lines 151-152). The runtime local-document-field.mjs is a default-exported value class (extends DocumentIdField). The other six are exposed as constructable values; this one is not, so consumers cannot construct it via the runtime API the way they can the siblings. The docstring explains the StringField modeling choice but not the absence of the value class. Genuine, minor coverage gap."}],"module/data/fields/typed-collection-field.d.mts":[{"severity":"low","axis":"shortcut","member":"ModelRegistry","fix":"Drop the `| never`: `type ModelRegistry = Record<string, dnd5e.types.fields.AnyDataModelConstructor>;`","note":"low — unverified"}],"module/data/item/class.d.mts":[{"severity":"low","axis":"seam","member":"dnd5e.types.ItemProperty.ValidPropertyMap","fix":"Add `class: dnd5e.types.ItemProperty.Class.TypeKey;` to ValidPropertyMap (itemPropertiesByType.d.mts:110-119) and define an ItemProperty.Class namespace with DefaultTypes { sidekick: true }, OverrideTypes, Types, TypeKey following the sibling pattern.","note":"Confirmed real. itemPropertiesByType.d.mts:110-119 enumerates weapon/equipment/consumable/tool/feat/loot/container/spell but omits `class`, despite class.mjs:77 exposing a class property filter and config.mjs registering validProperties.class. This missing seam is the root cause forcing the bare-string typing above. Low severity is acceptable since it is the enabling seam rather than the surfaced defect."}],"module/data/item/container.d.mts":[{"severity":"low","axis":"coverage","member":"_migrateWeightlessData","fix":"Add `static _migrateWeightlessData(source: object): void;` to the class.","note":"Confirmed: defined at container.mjs:268-272 and called externally from documents/item.mjs:550 as ContainerData._migrateWeightlessData(source), so it is a public, externally-invoked API absent from the declaration. Low severity (niche migration helper)."}],"module/data/item/fields/item-type-field.d.mts":[{"severity":"low","axis":"correctness","member":"ItemTypeField.Schema","fix":"Add `initial: \"\"` to each StringField options object in Schema (value/subtype/baseItem).","note":"Confirmed. Runtime constructs each StringField with `initial: options.value ?? \"\"` / `options.subtype ?? \"\"` / `options.baseItem ?? \"\"` (mjs:16,19,22), so the default initial is `\"\"`. Schema declares only `{ required: true; blank: true }`, omitting `initial`, which infers the initial as `undefined` rather than `\"\"`. Low severity is appropriate (only affects inferred initial value type, not assignability)."}],"module/data/item/loot.d.mts":[{"severity":"low","axis":"coverage","member":"inventorySection","fix":"Add `static get inventorySection(): InventoryElement.InventorySectionDescriptor;` to LootData (the named type is reachable as InventoryElement.InventorySectionDescriptor in src/module/applications/components/inventory.d.mts).","note":"Confirmed. loot.mjs:81 defines loot-specific `static get inventorySection()` returning InventorySectionDescriptor; it is not on ItemDataModel base nor any folded template, so the mixin does not surface it and the declaration lacks it. The named type IS reachable (inventory.d.mts:262). Real but low; siblings also omit it (accepted-gap caveat noted by finder)."}],"module/data/item/race.d.mts":[{"severity":"low","axis":"fidelity","member":"movementLabels","fix":"Change to `get movementLabels(): Partial<Record<dnd5e.types.Movement.TypeKey, string>>;`","note":"low — unverified"}],"module/data/item/subclass.d.mts":[{"severity":"low","axis":"correctness","member":"BaseSchema.spellcasting.ability","fix":"Type ability options as { required: false; blank: true } to match the bare StringField defaults (StringField defaults required:false, blank:true).","note":"Confirmed: spellcasting-field.mjs:22 is `new StringField({ label })` with no required/blank, so required defaults to false. Schema asserts { required: true; blank: true }. The required:true is wrong (blank:true happens to match the default). Same divergence exists in the class sibling, but it is still a real correctness mismatch. Low severity is correct."}],"module/data/item/templates/activities.d.mts":[{"severity":"low","axis":"fidelity","member":"prepareFinalActivityData","fix":"Type `rollData` as the item roll-data type instead of `fvttUtils.AnyObject` — e.g. `Item5e.RollData<any>` (src/documents/item.d.mts:196) matching the generated `dnd5e.types.defs.documents.ItemRollData` (src/_gen/documents/_types.d.mts:122).","note":"Confirmed: runtime activities.mjs:308 JSDoc types it `ItemRollData`. A concrete named type exists (Item5e.RollData / generated ItemRollData). Per strictness mandate, real types are preferred over AnyObject. Low because it is an input param."},{"severity":"low","axis":"fidelity","member":"recoverUses","fix":"Type the optional `rollData?` param as the item roll-data type (Item5e.RollData<any> / ItemRollData) rather than `fvttUtils.AnyObject`.","note":"Confirmed: runtime activities.mjs:333 JSDoc types it `ItemRollData`. Same concrete type exists as finding 2. Low (optional input param)."},{"severity":"low","axis":"correctness","member":"preUpdateActivities","fix":"Change the `user` param from `foundry.documents.BaseUser` to `User.Implementation`.","note":"Confirmed: runtime activities.mjs:413 JSDoc is `@param {User} user`, and sibling templates physical-item.d.mts:49 and equippable-item.d.mts:23 both use `User.Implementation` (the project convention). Using the abstract BaseUser is inconsistent. Low severity (input param, convention)."},{"severity":"low","axis":"coverage","member":"_typeCriticalThreshold","fix":"Add `/** @internal */ get _typeCriticalThreshold(): number | null;` to the Methods interface, parallel to the declared `_typeAbilityMod` (line 29).","note":"Confirmed: runtime criticalThreshold getter (activities.mjs:79) reads `this._typeCriticalThreshold ?? null`. This is a direct structural parallel to `_typeAbilityMod` which IS read-and-declared in the .d.mts. Note: grep found no concrete-model override of `_typeCriticalThreshold` (unlike `_typeAbilityMod`, which weapon/consumable/spell override), so it is a read-only internal hook with no current definer — but declaring it matches the existing convention for `_typeAbilityMod`. Low severity, internal hook."}],"module/data/item/templates/identifiable.d.mts":[{"severity":"low","axis":"coverage","member":"_migrateData","fix":"Optionally add `static override _migrateData(source: fvttUtils.AnyObject): void;` to the class body to mirror the runtime override.","note":"low — unverified"}],"module/data/item/templates/item-type.d.mts":[{"severity":"low","axis":"fidelity","member":"itemCategories (instance)","fix":"Change instance getter to `get itemCategories(): Record<string, string>;`","note":"Confirmed: instance getter (runtime line 22-24) returns the static value typed `@type {Record<string, string>}` (line 16). Methods interface line 16 uses `Record<string, unknown>`; the precise type Record<string, string> demonstrably exists in the runtime JSDoc. Valid fidelity finding."}],"module/data/item/templates/physical-item.d.mts":[{"severity":"low","axis":"fidelity","member":"compendiumBrowserPhysicalItemFilters","fix":"Type as `[string, dnd5e.types.defs.applications.CompendiumBrowserFilterDefinitionEntry][]` (the reachable _gen entry alias, equivalently CompendiumBrowser.FilterDefinitionEntry).","note":"The richer entry type plainly exists and is reachable: src/module/applications/compendium-browser.d.mts:209 (CompendiumBrowser.FilterDefinitionEntry) and src/_gen/applications/_types.d.mts:43 (dnd5e.types.defs.applications.CompendiumBrowserFilterDefinitionEntry). Runtime JSDoc @returns confirms `[string, CompendiumBrowserFilterDefinitionEntry][]` (physical-item.mjs:58). Caveat: the project ALSO ships a deliberate lean stub `type CompendiumBrowserFilterDefinition = Map<string, unknown>` (_stubs.d.mts:262), and the base getter system-data-model.d.mts:37 uses it — so `unknown` for the entry value is not entirely unprincipled here. But per the strictness mandate, with the precise named type reachable, `unknown` is an avoidable fidelity shortcut. Low severity is correct."},{"severity":"low","axis":"coverage","member":"price.valueInGP","fix":"Surface the conditionally-derived field via a PhysicalItem.DerivedData interface (e.g. `{ price?: { valueInGP?: number } }`) passed as the DerivedData type param instead of EmptyObject, so `system.price.valueInGP` is reachable on prepared instances.","note":"Confirmed: preparePhysicalData() sets `this.price.valueInGP = Math.floor(value * multiplier)` (physical-item.mjs:194), only when a `conversion` exists (hence optional/derived). It is correctly NOT in Schema, but DerivedData defaults to EmptyObject (line 66), leaving `system.price.valueInGP` unreachable. `valueInGP` appears nowhere in src/. Genuine reachability gap. Tempering: sibling templates (equippable-item, mountable) likewise default DerivedData=EmptyObject without surfacing their derived fields, so this is a consistent project posture, not a one-off omission. Low severity is appropriate."}],"module/data/item/templates/starting-equipment.d.mts":[{"severity":"low","axis":"fidelity","member":"EquipmentEntryData.CATEGORIES","fix":"Type as `Record<\"armor\"|\"currency\"|\"focus\"|\"tool\"|\"weapon\", { label?: string; config: string }>`.","note":"Confirmed at mjs:140-160: fixed object keyed by armor/currency/focus/tool/weapon, with currency lacking a label (so `label?` optional is correct). .d.mts line 56 widens to `Record<string, ...>`, losing the closed key set used by blankLabel/categoryLabel/keyOptions indexing on this.type. Real, low."}],"module/data/item/tool.d.mts":[{"severity":"low","axis":"correctness","member":"ToolData.abilityMod","fix":"Type the return as dnd5e.types.Ability.TypeKey (drop the | null).","note":"Confirmed: runtime returns `this.ability || \"int\"`, so any falsy ability (incl. the schema's blank \"\") yields \"int\". The getter never returns null, so the declared `| null` is unreachable. Low severity is correct."}],"module/data/journal/spells.d.mts":[{"severity":"low","axis":"correctness","member":"GROUPING_MODES","fix":"Type the static as `Record<\"none\"|\"alphabetical\"|\"level\"|\"school\", string>`, and ideally narrow the grouping StringField value to that key union (it uses `choices: this.GROUPING_MODES` at runtime).","note":"Confirmed: runtime GROUPING_MODES (spells.mjs:53-58) has a fixed closed key set none/alphabetical/level/school and is fed as `choices` to the grouping StringField (spells.mjs:25-26). The .d.mts types it as bare `Record<string, string>` (line 38) and the grouping StringField (line 47) carries no choices-derived value union, losing the known keys. Low severity is appropriate."}],"module/data/journal/subclass.d.mts":[{"severity":"low","axis":"correctness","member":"BaseSchema.item","fix":"Type as `foundry.data.fields.StringField<{ required: true }>`.","note":"low — unverified"}],"module/data/region-behavior/difficult-terrain.d.mts":[{"severity":"low","axis":"coverage","member":"_getTerrainEffects","fix":"Add `_getTerrainEffects(token: foundry.canvas.placeables.Token.Implementation, segment: object): { name: string }[];` to the class body.","note":"low — unverified"},{"severity":"low","axis":"coverage","member":"events","fix":"Add `static override events: Record<string, (event: any) => Promise<void>>;` to the class body (or accept the established sibling convention of omitting it).","note":"low — unverified"}],"module/data/region-behavior/rotate-area.d.mts":[{"severity":"low","axis":"correctness","member":"BaseSchema.positions","fix":"Add the second type arg `, { initial: [{ angle: 0 }] }` to the `positions` ArrayField.","note":"Confirmed: runtime rotate-area.mjs:54-56 constructs the positions ArrayField with `{ initial: [{ angle: 0 }] }` as the options arg, but the decl's ArrayField has no options type arg, so the field's initial/required metadata diverges from runtime (default empty-array initial vs the real seeded entry). Genuine low-severity correctness/fidelity divergence."}],"module/data/settings/transformation-setting.d.mts":[{"severity":"low","axis":"correctness","member":"Transformation.Schema.spellLists","fix":"Drop the fabricated options: `SetField<dnd5e.types.fields.RestrictedStringField<dnd5e.types.SpellListType.TypeKey>>` to match the runtime constructor.","note":"Confirmed: runtime is plain `new SetField(new StringField())` with no options (transformation-setting.mjs:31). The fabricated `{ required: true; blank: false }` is wrong — a plain StringField defaults to `blank: true` (fvtt-types fields.d.mts:1876), so `blank: false` excludes `\"\"` from the value union incorrectly. Keeping the `SpellListType.TypeKey` union is correct per the strictness mandate; the established no-options pattern is mirrored in _shims.d.mts:33 (`SetField<RestrictedStringField<Value>>`)."},{"severity":"low","axis":"fidelity","member":"createFormCategories","fix":"Type the return as `Array<{ category: string; title: string; hint: string; settings: object[] }>`.","note":"Confirmed: runtime JSDoc (transformation-setting.mjs:67) and code (86-101) produce the concrete shape `{ category, title, hint, settings: object[] }[]`; .d.mts returns bare `object[]`. The named shape is demonstrably present."},{"severity":"low","axis":"fidelity","member":"createFormCategories / createFormField (options.host)","fix":"Type `host?: globalThis.Actor.Implementation` in both option bags.","note":"Confirmed: runtime JSDoc types `host` as `Actor5e` (lines 65, 111). Convention maps Actor5e to `globalThis.Actor.Implementation`, used by sibling BastionSetting (bastion-setting.d.mts:26). Currently `host?: unknown`."},{"severity":"low","axis":"correctness","member":"createFormField (options required)","fix":"Make the third arg required: `createFormField(name: string, field: foundry.data.fields.DataField.Any, options: { prefix?: string; host?: globalThis.Actor.Implementation }): object;`","note":"Confirmed: runtime `createFormField(name, field, { prefix=\"\", host })` destructures the options object with NO `={}` default (transformation-setting.mjs:115), so omitting it throws at runtime. The .d.mts incorrectly marks it `options?`. Contrast createFormCategories which DOES have `={}` (line 69), so its optional marker is correct."}],"module/data/shared/damage-field.d.mts":[{"severity":"low","axis":"shortcut","member":"scaledFormula","fix":"Reference the real Scaling type (e.g. `number | (typeof import(\"../../../documents/scaling.d.mts\"))[\"default\"][\"prototype\"]`) and drop the stale 'until the mirror lands' comment.","note":"Confirmed: runtime scaledFormula(increase) signature is `number|Scaling` (damage-field.mjs:104,108-109), and the Scaling mirror DOES exist at src/documents/scaling.d.mts (default-exported class with `get increase(): number`). The .d.mts uses `number | { increase: number }` (line 70) with a 'until the documents/scaling.mjs mirror lands' comment (lines 66-67) that is now stale. Genuine corner-cut; low severity since the structural type is still compatible."},{"severity":"low","axis":"fidelity","member":"<file>","fix":"Update the header comment to note `types` is tightened to `Damage.TypeKey` (RestrictedStringField) and only `modifiers` is plain `Set<string>`.","note":"Confirmed self-inconsistency: header comment (lines 9-12) claims both `types`/`modifiers` are plain `Set<string>` (untyped StringField), but the Schema tightens `types` to `RestrictedStringField<dnd5e.types.Damage.TypeKey>` (line 23) per the strictness mandate while leaving `modifiers` plain (line 29). The comment contradicts the file's own (correct) typing. This is a doc-staleness nit rather than a true bare-string fidelity gap (the member itself is correctly typed), so low severity; kept real because it is a genuine inaccuracy."}],"module/data/shared/duration-field.d.mts":[{"severity":"low","axis":"shortcut","member":"<file>","fix":"Rewrite lines 8-11 to state `units` is keyed by `dnd5e.types.TimePeriod.TypeKey` via RestrictedStringField, and drop the `(REPORTED)`/`falls back to a plain StringField` text.","note":"low — unverified"}],"module/data/shared/uses-field.d.mts":[{"severity":"low","axis":"coverage","member":"UsesField.getStatblockLabel","fix":"Add `static getStatblockLabel(this: ItemDataModel | BaseActivityData): string;` to the class body.","note":"Confirmed. Runtime uses-field.mjs:142 `static getStatblockLabel()` returning string, called internally via `UsesField.getStatblockLabel.call(this)` at line 69. grep found no external callers, so it is effectively an internal prepareData helper, but it is a non-private static on the public surface and is absent from the empty class body. Low is correct."}],"types/expandable.d.mts":[{"severity":"low","axis":"shortcut","member":"RemoveIndexSignatures / OmitIndex","fix":"Delete the local `RemoveIndexSignatures` (39-41) and `OmitIndex` (26-32) and rely on `fvttUtils.RemoveIndexSignatures`, or have `ExtractKeys`/`MergeOverrideDefinition` use the local versions consistently.","note":"low — unverified"},{"severity":"low","axis":"shortcut","member":"IsNever","fix":"Either route `FilterNever`/`ExtractKeys`'s never-checks through `IsNever`, or remove the unused `IsNever` alias.","note":"low — unverified"}],"module/config/ability.d.mts":[{"severity":"low","axis":"correctness","member":"Config.fullKey","fix":"Change `fullKey?: string;` to `fullKey: string;`.","note":"Confirmed. Runtime typedef _types.mjs:12 declares `@property {string} fullKey` with no brackets (required), and every entry in DND5E.abilities (including hon/san) supplies fullKey. The interface marks it optional. Genuine optionality mismatch; low severity is appropriate."}],"module/config/conditionEffects.d.mts":[{"severity":"low","axis":"fidelity","member":"DND5EConfig.conditionEffects","fix":"Type entries as Set<dnd5e.types.Condition.TypeKey | `exhaustion-${number}` | (string & {})> to capture the known condition union while still allowing extra status ids.","note":"low — unverified"}],"module/config/conditionTypes.d.mts":[{"severity":"low","axis":"coverage","member":"Config","fix":"Add an index signature or optional id/changes/duration fields to Config (or extend a reachable foundry status-effect type) to admit the foundry-base members.","note":"low — unverified"}],"module/config/_core-funnels.d.mts":[{"severity":"low","axis":"coverage","member":"CONFIG.Token (movement.TerrainData / movement.costAggregator)","fix":"Add to namespace CONFIG a 'interface TokenConfig'/'namespace Token { interface Movement }' merge typing movement.TerrainData (typeof TerrainData5e) and movement.costAggregator, or document why they live elsewhere.","note":"low — unverified"}],"module/config/encumbrance.d.mts":[{"severity":"low","axis":"fidelity","member":"DND5EConfig.encumbrance.baseUnits","fix":"Type baseUnits as Record<string, { imperial: dnd5e.types.WeightUnit.TypeKey; metric: dnd5e.types.WeightUnit.TypeKey }> instead of Record<string, dnd5e.types.DefaultUnit.Config>.","note":"low — unverified"}],"module/config/facilities.d.mts":[{"severity":"low","axis":"fidelity","member":"Facility.Config.advancement","fix":"advancement: Partial<Record<dnd5e.types.Facility.Type.TypeKey, Record<number, number>>>;","note":"Confirmed. config.mjs:527-530 keys advancement by 'basic'/'special', which is exactly dnd5e.types.Facility.Type.TypeKey defined in this same file (lines 72-79). Currently bare Record<string,...>. Reachable named union exists; minor but valid fidelity tightening. Partial is safe (both keys present at runtime but expandable)."}],"module/config/limitedUsePeriods.d.mts":[{"severity":"low","axis":"fidelity","member":"DND5EConfig.limitedUsePeriods.recoveryOptions","fix":"Type value as `dnd5e.types.LimitedUsePeriod.TypeKey | \"recharge\"` instead of bare `string`.","note":"low — unverified"}],"module/config/neverBlockStatuses.d.mts":[{"severity":"low","axis":"fidelity","member":"neverBlockStatuses","fix":"Change to Set<dnd5e.types.StatusEffect.TypeKey | dnd5e.types.Condition.TypeKey | string>.","note":"low — unverified"}],"module/config/skill.d.mts":[{"severity":"low","axis":"correctness","member":"Skill.Config.fullKey","fix":"Make `fullKey` required: change `fullKey?: string;` to `fullKey: string;`.","note":"Confirmed: typedef (_types.mjs:468) declares fullKey as required `@property {string} fullKey` (no optional brackets), and all 18 runtime skill entries define fullKey. The .d.mts marks it fullKey?: string. Genuine correctness mismatch; low severity since the field is always present at runtime."}],"module/config/_stubs.d.mts":[{"severity":"low","axis":"seam","member":"Spellcasting.Level","fix":"Consolidate to a single spell-level domain and have both the config funnel (spellLevels.d.mts) and the creature template (creature.d.mts:67) key off it; eliminate the divergent string-key '0'..'9' vs numeric 0..9 duplication.","note":"Confirmed. _stubs declares Spellcasting.Level (string keys \"0\"-\"9\") consumed by creature.d.mts:67 (SpellLevelKey), while spellLevels.d.mts independently declares SpellLevel (numeric keys 0-9) keying DND5EConfig.spellLevels with its own OverrideTypes. Two redundant declarations of the same domain with divergent key shapes and separate merge points; widening one does not affect the other. Low severity is fair."},{"severity":"low","axis":"seam","member":"Language.CommunicationDefaultTypes","fix":"Pick one location for communication modes and have both _fields.d.mts:130 and the communicationTypes config funnel key off the same namespace (and OverrideTypes merge point).","note":"Confirmed. _stubs nests communication modes under Language.Communication* (consumed by _fields.d.mts:130 and _gen _types.d.mts:161 as Language.CommunicationTypeKey), while communicationTypes.d.mts declares a separate top-level CommunicationType namespace keying DND5EConfig.communicationTypes. Two parallel merge points for one domain; the documented CommunicationType.OverrideTypes seam will not affect the consumed Language.CommunicationTypeKey. Low severity is fair."}],"module/config/timeUnits.d.mts":[{"severity":"low","axis":"fidelity","member":"TimeUnit.Config.timeComponent","fix":"Change to `timeComponent?: keyof foundry.data.CalendarData.TimeComponents;`","note":"low — unverified"}],"module/applications/activity/enchant-sheet.d.mts":[{"severity":"low","axis":"coverage","member":"tabGroups","fix":"Add `override tabGroups: { sheet: string; activation: string; effect: string; };` inside the `declare class EnchantSheet` body.","note":"low — unverified"}],"module/applications/activity/heal-sheet.d.mts":[{"severity":"low","axis":"fidelity","member":"HealSheet.RenderContext.scalingOptions","fix":"Change to `{ value: \"\" | dnd5e.types.DamageScalingMode.TypeKey; label: string }[]`","note":"Runtime (heal-sheet.mjs lines 39-42) produces a leading entry with value \"\" followed by Object.entries(CONFIG.DND5E.damageScalingModes) entries whose value is dnd5e.types.DamageScalingMode.TypeKey (defined in src/module/config/damageScalingModes.d.mts). No selected field is present. FormSelectOption[] misrepresents value as optional string. The precise union type exists in this repo."}],"module/applications/activity/summon-usage-dialog.d.mts":[{"severity":"low","axis":"coverage","member":"static PARTS","fix":"Add `static PARTS: typeof ActivityUsageDialog.PARTS & { creation: foundry.applications.api.HandlebarsApplicationMixin.HandlebarsTemplatePart & ApplicationV2Mixin.ApplicationContainerParts };` to the class body.","note":"low — unverified"}],"module/applications/activity/transform-usage-dialog.d.mts":[{"severity":"low","axis":"coverage","member":"_prepareCreationContext","fix":"Add `_prepareCreationContext(context: RenderContext, options: fvttUtils.DeepPartial<RenderOptions>): Promise<RenderContext>;` to the class body, mirroring the parent's declaration.","note":"low — unverified"}],"module/applications/actor/api/base-actor-sheet.d.mts":[{"severity":"low","axis":"fidelity","member":"RenderContext.config","fix":"Change `config: object` to `config: dnd5e.types.DND5EConfig` in the `RenderContext` interface.","note":"Runtime line 214 assigns `context.config = context.CONFIG`, where `CONFIG` is typed as `dnd5e.types.DND5EConfig` on `ApplicationV2Mixin.RenderContext` (confirmed in application-v2-mixin.d.mts). The comment 'Temporary patch until all templates have been updated' refers to the assignment pattern itself, not the type — the value is definitively `DND5EConfig` at runtime. Typing as bare `object` is a fidelity shortcut since the concrete type is reachable."}],"module/applications/actor/character-sheet.d.mts":[{"severity":"low","axis":"fidelity","member":"RenderContext.spellcasting","fix":"Change `ability: { mod: number; ability: string }` to `ability: { mod: number; ability: dnd5e.types.Ability.TypeKey }` in `RenderContext.spellcasting`.","note":"Confirmed genuine. `sc.ability` (from `item.spellcasting`) is used as a key into `this.actor.system.abilities` (keyed by `Ability.TypeKey`) and compared to `this.actor.system.attributes.spellcasting`. It is definitionally an ability type key. `dnd5e.types.Ability.TypeKey` exists and is the correct precise type. Bare `string` is too wide."}],"module/applications/actor/config/habitat-config.d.mts":[{"severity":"low","axis":"fidelity","member":"RenderContext.custom.field","fix":"Change `field: foundry.data.fields.DataField.Any` to `field: foundry.data.fields.StringField`.","note":"The runtime reads the field from `this.document.system.schema.fields.details.fields.habitat.fields.custom`, which is declared as `new StringField({ required: true })` in _research/dnd5e/module/data/actor/npc.mjs:95. The concrete type is known and reachable; `DataField.Any` is an unnecessary widening. ArmorClassConfig uses `foundry.data.fields.StringField` for the same pattern at line 39 of its declaration."},{"severity":"low","axis":"coverage","member":"title","fix":"Add `get title(): string;` to the class body of `HabitatConfig`.","note":"The runtime class explicitly overrides `get title()` at line 24 of habitat-config.mjs. The declaration's class body is empty, so the override is not reflected. ArmorClassConfig and other config sheets with the same `get title()` override pattern do declare `get title(): string` in their class bodies. This is a genuine, if minor, coverage gap."}],"module/applications/actor/config/hit-points-config.d.mts":[{"severity":"low","axis":"correctness","member":"RenderContext.showCalculation","fix":"Change `showCalculation: boolean;` to `showCalculation: number | foundry.data.fields.DataField.Any | false;` (or at minimum `showCalculation: number | object | false;`).","note":"Runtime line 77 assigns `context.classes.length || context.fields.bonuses`. The JS `||` operator returns the first truthy operand or the last value, yielding `number | DataField | 0 | false` — not `boolean`. While consumers typically treat it as truthy/falsy, the strict type is wrong. Severity is low because the practical impact on callers is minimal."},{"severity":"low","axis":"fidelity","member":"RenderContext.otherFields","fix":"Change `field: object` to `field: foundry.data.fields.DataField.Any` in the `otherFields` array element type.","note":"Runtime line 82 assigns `field: context.fields[p]` where fields are DataField instances from the hp schema. habitat-config.d.mts line 21 uses `foundry.data.fields.DataField.Any` for an identical pattern. Using `object` is a fidelity shortcut when the precise type is reachable and used consistently across sibling files."}],"module/applications/actor/config/initiative-config.d.mts":[{"severity":"low","axis":"correctness","member":"RenderContext.ability.global.field","fix":"Change `field: foundry.data.fields.DataField.Any` in `ability.global` to `field: foundry.data.fields.DataField.Any | undefined`.","note":"Runtime line 58 uses `this.document.system.schema.fields.bonuses?.fields.abilities.fields.check` with optional chaining on `bonuses?`, meaning the field is `undefined` when the actor subtype has no bonuses schema group. The declaration types it as non-optional `DataField.Any`."}],"module/applications/actor/config/languages-config.d.mts":[{"severity":"low","axis":"fidelity","member":"RenderContext.communication[].fields","fix":"Change `fields: foundry.data.fields.DataSchema` to `fields: { units: dnd5e.types.fields.RestrictedStringField<dnd5e.types.MovementUnit.TypeKey | \"\", { required: true; blank: true }>; value: foundry.data.fields.NumberField<{ required: true; min: 0 }> }`","note":"Genuine but severity downgraded from medium to low. The runtime assigns `context.fields.communication.model.fields` where `.model` is the inner `SchemaField<{ units, value }>` of the `MappingField`. That inner schema is precisely defined in src/module/data/actor/templates/_fields.d.mts (`CreatureSchema.languages.communication`). `foundry.data.fields.DataSchema` (i.e. `Record<string, DataField.Any>`) is looser than needed — the exact field types are known and reachable. However, the fix must be an inline type because `MappingField` in the repo is typed as an `ObjectField` alias without a `.model` property, so the precise type cannot be derived structurally from the existing `MappingField` declaration; it must be hardcoded inline. This is still a real fidelity improvement but the hardcoded inline nature limits its score."}],"module/applications/actor/group-sheet.d.mts":[{"severity":"low","axis":"fidelity","member":"RenderContext.members","fix":"Extend the member object shape to add optional fields: `canView?: boolean; classes?: globalThis.Item.Implementation[]; skills?: Record<string, { css: string; label: string; passive: number; total: number }>; underlay?: string;`. These are present in members context but not inventory context, so marking them optional is correct.","note":"Runtime `_prepareMembersContext` (lines 152-154, 233) adds `canView`, `classes`, `skills`, and `underlay` to each member object. These are absent from the declared member shape. The `members` array is shared between inventory and members contexts (where not all fields are present), making optional typing correct. The precise skill shape `{ css, label, passive, total }` is confirmed at runtime line 271-274."}],"module/applications/actor/npc-sheet.d.mts":[{"severity":"low","axis":"shortcut","member":"RenderContext.sections","fix":"Change `sections?: any[]` to `sections?: object[]`.","note":"Runtime line 229 confirms it is an array (from `prepareSections(Object.values(sections))`), so `any[]` is weaker than necessary. The base declares `sections?: object` (not an array), so the NPC override narrows the base correctly as an array. However, `any[]` is weaker than `object[]`. Downgraded from medium to low because the precise element type comes from a custom-element method with no named return type in the repo."},{"severity":"low","axis":"shortcut","member":"RenderContext.portrait","fix":"Change `portrait?: any` to `portrait?: object`.","note":"Base `_preparePortrait` is declared as `Promise<object>`, and runtime line 258 assigns `context.portrait = await this._preparePortrait(context)`. Typing as `any` discards the known constraint."},{"severity":"low","axis":"shortcut","member":"RenderContext.abilities","fix":"Change `abilities?: any` to `abilities?: object[]`.","note":"Base `_prepareAbilities` is declared as returning `object[]`. Runtime line 268 assigns `context.abilities = this._prepareAbilities(context)`. Using `any` is weaker."},{"severity":"low","axis":"shortcut","member":"RenderContext.traits","fix":"Change `traits?: any` to `traits?: Record<string, object[]>`.","note":"Base `_prepareTraits` is declared as returning `Record<string, object[]>`. Runtime line 387 assigns `context.traits = this._prepareTraits(context)`. Using `any` discards the known structure."},{"severity":"low","axis":"shortcut","member":"RenderContext.senses / RenderContext.skills / RenderContext.tools","fix":"Change `senses?: any[]; skills?: any[]; tools?: any[]` to `senses?: object[]; skills?: object[]; tools?: object[]`.","note":"Base `_prepareSenses` and `_prepareSkillsTools` are declared as returning `object[]`. Runtime lines 359, 368, 370 confirm the assignments. All three use `any[]` which is weaker."},{"severity":"low","axis":"shortcut","member":"RenderContext.speed[].icons","fix":"Change `icons?: any[]` to `icons?: Array<{ icon: string; label: string }>` inside the speed element type.","note":"Runtime lines 378-381 show the only icon produced is `{ icon: 'fas fa-cloud', label: _loc('DND5E.MOVEMENT.Hover') }` — a fixed shape with two string properties. The `any[]` annotation hides this completely."}],"module/applications/actor/party-request-dialog.d.mts":[{"severity":"low","axis":"correctness","member":"Recipient","fix":"Change the union member to { value: string; label: string } (drop '| null' from value)","note":"Runtime line 74 maps u.id directly — Foundry Document IDs are always non-null strings. The typing value: string | null widens beyond what the runtime produces and is a genuine correctness issue, though low-impact in practice."},{"severity":"low","axis":"fidelity","member":"Recipient","fix":"Narrow to { rule: true } in the union: Array<{ rule: true } | { value: string; label: string }>","note":"Runtime line 80 always passes the literal { rule: true }. Typing it as { rule: boolean } allows callers to observe rule: false which is never emitted. A genuine fidelity imprecision, though minor since consumers typically just check for the presence of 'rule'."}],"module/applications/actor/rest/base-rest-dialog.d.mts":[{"severity":"low","axis":"shortcut","member":"BaseRestDialog.RenderContext.result","fix":"Declare `result?: unknown` on the class body and change `result: any` to `result?: unknown` in `RenderContext`.","note":"`result: this.result` in `_prepareContext` (line 133) references a property never declared on `BaseRestDialog` or any subclass (confirmed by reading LongRestDialog and ShortRestDialog). Without a class-level declaration `this.result` is `undefined` at runtime; `any` silences the resulting type error rather than expressing the intent. `result?: unknown` is accurate."}],"module/applications/actor/transform-dialog.d.mts":[{"severity":"low","axis":"fidelity","member":"RenderContext.categories","fix":"Change to `ReturnType<TransformationSetting[\"createFormCategories\"]>` (resolves to `object[]`)","note":"The runtime assigns `this.#settings.createFormCategories(...)` directly to `context.categories`. The declared return type of `createFormCategories` is `object[]` in transformation-setting.d.mts. Using `any` is strictly worse than `object[]` because `any` bypasses all type checking. The fix is modest — `object[]` is still loose — but removing bare `any` is a real improvement. Severity downgraded to low because `object[]` is not a precise domain type, just the declared return type."},{"severity":"low","axis":"correctness","member":"settings","fix":"Change return type of `settings` getter to `TransformationSetting` (drop `| null`)","note":"The runtime constructor always sets `#settings` via `this.options.transform.settings?.clone() ?? new TransformationSetting()`, so `#settings` is never null after construction. All internal usages (lines 140, 144, 158, 238, 241) access `this.#settings` without any null-guard, confirming the non-null invariant. The `| null` in the getter return type is inaccurate. However, the class field itself is initialized to `null` before the constructor runs (line 75), so the field can technically be null during field initialization — but this is not observable through the public getter. Severity is low rather than medium because the `| null` annotation follows the JSDoc on the private field and is a conservative defensive typing, not a dangerous misrepresentation."}],"module/applications/advancement/ability-score-improvement-config.d.mts":[{"severity":"low","axis":"fidelity","member":"RenderContext.points.key","fix":"Change `key: string` to `key: \"points\"` since the runtime always assigns the literal `\"points\"` at line 59 with no variation.","note":"Runtime always assigns `key: \"points\"` literally (line 59). Typing it as bare `string` loses the precise literal type. The literal is statically unambiguous and the fix is trivial. Genuine but low-severity fidelity gap."}],"module/applications/advancement/advancement-config.d.mts":[{"severity":"low","axis":"coverage","member":"title","fix":"Add: get title(): string;","note":"Runtime lines 61-63 override the title getter to compose a localised string from item name and advancement type. It is absent from the declaration. Low severity is appropriate."},{"severity":"low","axis":"coverage","member":"activateListeners","fix":"Add: activateListeners(html: JQuery | HTMLElement): void;","note":"Runtime lines 118-127 override activateListeners to wire the delete-item click handler and multi-select change listeners. It is absent from the declaration. Low severity is appropriate."}],"module/applications/advancement/advancement-flow.d.mts":[{"severity":"low","axis":"correctness","member":"_advancementId","fix":"Change `_advancementId: string;` to `protected _advancementId: string;` to reflect the runtime `@private` JSDoc intent.","note":"Runtime JSDoc at advancement-flow.mjs line 24 explicitly marks `_advancementId` as `@private`. The declaration exposes it as a public instance field, which contradicts the stated runtime intent. A `protected` modifier is the closest safe TypeScript equivalent (since subclasses may inherit it) and accurately signals it is not part of the public API."}],"module/applications/advancement/advancement-manager.d.mts":[{"severity":"low","axis":"coverage","member":"id","fix":"Add `override get id(): string;` to the class body.","note":"The runtime overrides `get id()` at lines 115-117 returning `actor-${this.actor.id}-advancement`. The override is absent from the declaration file. While the return type is still string (same as the base), the override signals a deliberate contract — omitting it means consumers cannot rely on the deterministic id pattern. Severity remains low as the type is unchanged from the base."}],"module/applications/advancement/advancement-migration-dialog.d.mts":[{"severity":"low","axis":"coverage","member":"static DEFAULT_OPTIONS","fix":"Add `static DEFAULT_OPTIONS: { classes: string[]; actions: Record<string, Function>; advancements: dnd5e.types.Advancement.Instance[]; position: { width: number } };` to the class body.","note":"low — unverified"}],"module/applications/advancement/hit-points-flow.d.mts":[{"severity":"low","axis":"fidelity","member":"RenderContext.data.value","fix":"Change `value: number | string` to `value: number | \"\"`","note":"low — unverified"},{"severity":"low","axis":"fidelity","member":"RenderContext.hp.total","fix":"Change `total: number | string` to `total: number | \"—\"`","note":"low — unverified"}],"module/applications/advancement/item-choice-flow.d.mts":[{"severity":"low","axis":"fidelity","member":"RenderContext.sections","fix":"Extract a SectionEntry interface capturing `{ header: string; items: Array<{ id: string; img: string; name: string; uuid: string; checked: boolean; disabled?: boolean; dropped?: boolean; previouslyReplaced?: boolean; replaced?: boolean }>; isCurrentLevel?: boolean }` and type sections as `IterableIterator<SectionEntry>`.","note":"Confirmed real. The runtime at lines 110-176 explicitly constructs section objects with a concrete, repeated structure before calling .values() at line 177. The bare `object` element type loses all that structure. The better type clearly exists in the runtime code and is not dynamic or untyped."}],"module/applications/advancement/scale-value-config.d.mts":[{"severity":"low","axis":"shortcut","member":"RenderContext.type","fix":"Replace `& Record<string, any>` with the explicit spread shape. The known structure is `{ label: string; hint: string; identifier: string; isNumeric: boolean; fields: foundry.data.fields.DataSchema; options: { value: string; label: string }[] }` — the `& Record<string, any>` catch-all hides a fully known type.","note":"Confirmed: `type.metadata` is always `{ label, hint, identifier, isNumeric }` (scale-value-data.mjs:110-115); the spread at line 68-72 adds `fields` and `options`. No dynamic/unknown keys are added, so `& Record<string, any>` is a pure shortcut."},{"severity":"low","axis":"coverage","member":"_processSubmitData","fix":"Add `protected _processSubmitData(event: SubmitEvent, submitData: object): Promise<void>;` to the class body.","note":"Confirmed: the runtime overrides `_processSubmitData` at lines 175-193 with non-trivial type-conversion logic (handles the case where the scale value type changes mid-submit). The override is absent from the declaration. Severity kept low because the base signature still applies in practice."}],"module/applications/advancement/scale-value-flow.d.mts":[{"severity":"low","axis":"coverage","member":"ScaleValueFlow.PARTS","fix":"Add `static override PARTS: (typeof AdvancementFlow)[\"PARTS\"];` (or the full typed form) inside the `declare class ScaleValueFlow` body.","note":"low — unverified"}],"module/applications/advancement/size-flow.d.mts":[{"severity":"low","axis":"fidelity","member":"RenderContext.size.field","fix":"Change `field: foundry.data.fields.StringField;` to `field: dnd5e.types.fields.RestrictedStringField<dnd5e.types.ActorSize.TypeKey, { required: true; blank: false }>;`","note":"low — unverified"}],"module/applications/api/dialog.d.mts":[{"severity":"low","axis":"shortcut","member":"Button","fix":"Split into an input shape (no `cssClass`) and a rendered-context shape (adds `cssClass?: string`), or at least document that `cssClass` is a computed field added by `_prepareFooterContext` and should not be supplied by callers.","note":"Confirmed as a real shortcut but downgraded to low severity. `cssClass` is computed in `_prepareFooterContext` as `cssClass: button.class` and is never supplied by callers in the runtime codebase. Accepting it as an input field silently misrepresents the interface, but since it is marked optional (`cssClass?`) no callers are broken and no type errors occur in practice."}],"module/applications/api/drag-drop-mixin.d.mts":[{"severity":"low","axis":"fidelity","member":"_dropBehavior","fix":"Change `data?: any` to `data?: object` in all three protected method signatures: `_dropBehavior`, `_allowedDropBehaviors`, and `_defaultDropBehavior`.","note":"low — unverified"}],"module/applications/api/primary-sheet-mixin.d.mts":[{"severity":"low","axis":"coverage","member":"BasePrimarySheet5e._configureRenderParts","fix":"Add `protected _configureRenderParts(options: object): Record<string, foundry.applications.api.HandlebarsApplicationMixin.HandlebarsTemplatePart>` to `BasePrimarySheet5e`. The runtime override at line 103 conditionally removes tab parts based on TABS condition functions.","note":"Confirmed: runtime defines `_configureRenderParts` at line 103; absent from the declaration. Low severity because callers rarely need to override or intercept this specific method."}],"module/applications/api/pseudo-document-sheet.d.mts":[{"severity":"low","axis":"fidelity","member":"Configuration","fix":"Replace `metadata: { name: string }` with `metadata: dnd5e.types.defs.documents.mixins.PseudoDocumentsMetadata` in the `Configuration.document` inline shape.","note":"The runtime uses `this.document.metadata.label` at lines 111 and 175 of the runtime source. The current typing only includes `name`, missing `label`. The named type `dnd5e.types.defs.documents.mixins.PseudoDocumentsMetadata` (in src/_gen/documents/mixins/_types.d.mts) defines both `name: string` and `label: string` — it is the precise existing type for this shape. Using the inline `{ name: string }` is both incomplete and misses the reachable named type."},{"severity":"low","axis":"shortcut","member":"PseudoDocumentSheet","fix":"Once pseudo-document mixin types are available, replace `Document extends foundry.abstract.DataModel.Any` with a tighter bound reflecting the actual pseudo-document shape (ActivityBehavior & DataModel or similar) and remove the TODO comment.","note":"The file's own comment explicitly says 'the Document bound is loose (DataModel.Any) pending the pseudo-document mixin port', confirming this is a deliberate placeholder. Per the review axes a 'loose pending' TODO comment qualifies as a shortcut. Severity is low because the limitation is acknowledged and the bound is not structurally wrong — it merely prevents the compiler from enforcing pseudo-document-specific members on the `document` getter and submit methods."}],"module/applications/calendar/calendar-hud.d.mts":[{"severity":"low","axis":"coverage","member":"_preparePartContext","fix":"Add `_preparePartContext(partId: string, context: RenderContext, options: RenderOptions): Promise<RenderContext>;` to the CalendarHUD class body.","note":"Confirmed: runtime line 192 overrides `_preparePartContext` with `@inheritDoc` to filter context.buttons by position for 'startButtons' and 'endButtons' parts. Missing from the declaration."},{"severity":"low","axis":"coverage","member":"_onClickAction","fix":"Add `_onClickAction(event: PointerEvent, target: HTMLElement): void;` to the CalendarHUD class body.","note":"Confirmed: runtime line 290 has a `@override` annotation on `_onClickAction` that delegates to the private #buttons array and calls config.onClick. The override is absent from the declaration."}],"module/applications/compendium-browser.d.mts":[{"severity":"low","axis":"fidelity","member":"CompendiumBrowser.BATCHING","fix":"Change `static BATCHING: Record<string, number>;` to `static BATCHING: { readonly MARGIN: number; readonly SIZE: number };`.","note":"The runtime defines exactly two keys (`MARGIN` and `SIZE`) and both are accessed by name in the implementation (lines 685, 901, 906). The precise shape is statically known. Severity is low (not medium) because the runtime's own JSDoc explicitly annotates this as `@type {Record<string, number>}`, meaning the declaration faithfully mirrors the documented type; the tighter shape is a project-mandate upgrade rather than a clear mismatch."},{"severity":"low","axis":"fidelity","member":"RenderContext.types","fix":"Change `types: SelectChoices | Record<string, any>;` to `types: SelectChoices | Record<string, SelectChoices.Entry>;` in `interface RenderContext`.","note":"The `Record<string, any>` arm IS materializable: at runtime line 495 `context.types` is set to `context.types.physical.children`, and `SelectChoices.Entry.children` is typed as `SelectChoices | Record<string, Entry>`. So both arms of the union are reachable and the union itself is correct. The real fidelity issue is that `any` should be `SelectChoices.Entry` — the precise entry type exists in this repo. The original finding's proposed fix (removing the union entirely) is wrong; the union must be kept but tightened."}],"module/applications/components/activities.d.mts":[{"severity":"low","axis":"correctness","member":"_onAction","fix":"Add `private` modifier: `private _onAction(target: HTMLElement, action: string, options?: { event?: PointerEvent }): Promise<void>;`","note":"Runtime JSDoc at line 81 explicitly marks this `@private`. TypeScript `private` in a `.d.mts` file has real compile-time enforcement value and correctly reflects the intended API surface. Leaving it public misleads consumers. Severity downgraded from medium to low because the method has a leading underscore (conventional JS signal) and this is an HTML custom element unlikely to be subclassed in ways that depend on calling this method directly."}],"module/applications/components/adopted-stylesheet-mixin.d.mts":[{"severity":"low","axis":"fidelity","member":"_stylesheets","fix":"Change to `WeakMap<Document, CSSStyleSheet>`.","note":"The runtime JSDoc annotates the map as `WeakMap<WeakKey<Document>, CSSStyleSheet>` and the only key ever set or retrieved is `this.ownerDocument` (type Document). The declaration uses bare `WeakKey` (= object | symbol), which is wider than necessary. The narrower `Document` type exists and is the sole key used, making this a genuine fidelity shortcut, though with no practical runtime impact."}],"module/applications/components/checkbox.d.mts":[{"severity":"low","axis":"fidelity","member":"tagName","fix":"Change `static tagName: string;` to `static tagName: \"dnd5e-checkbox\";`.","note":"Confirmed genuine but low severity. Runtime line 22 sets the literal 'dnd5e-checkbox'. The base fvtt-types declaration uses bare string, but overriding with the literal is a valid narrowing that the strictness mandate supports. The literal is stable and reachable within this file."}],"module/applications/components/damage-application.d.mts":[{"severity":"low","axis":"coverage","member":"_onOpen","fix":"Add `override _onOpen(): void;` to the class body.","note":"Confirmed. Runtime line 397-399 overrides `_onOpen()` with a call to `this.buildTargetsList()`. The override is absent from the declaration."},{"severity":"low","axis":"coverage","member":"_onVisible","fix":"Add `override _onVisible(): void;` to the class body.","note":"Confirmed. Runtime line 404-406 overrides `_onVisible()` with a call to `this.buildTargetsList()`. The override is absent from the declaration."},{"severity":"low","axis":"fidelity","member":"tagName","fix":"Change `static tagName: string` to `static tagName: \"damage-application\"`","note":"Confirmed. Runtime line 28 assigns the literal `\"damage-application\"`. The literal type is reachable and fixed — this class is a concrete leaf, not a base meant to be subclassed with a different tag. Widening to `string` loses precision that the strictness mandate requires for fixed domain values."}],"module/applications/components/effect-application.d.mts":[{"severity":"low","axis":"fidelity","member":"tagName","fix":"Change to `static tagName: \"effect-application\";`","note":"The runtime assigns the string literal `\"effect-application\"` (line 17). Per the project's strictness mandate, domain values must use real literal/union types rather than bare `string`. The literal `\"effect-application\"` is the correct and reachable type here. Finding is genuine."}],"module/applications/components/effects.d.mts":[{"severity":"low","axis":"fidelity","member":"_onAction","fix":"Change `action: string` to `action: \"toggleCondition\" | \"create\" | \"delete\" | \"duplicate\" | \"edit\" | \"favorite\" | \"toggle\" | \"unfavorite\"`.","note":"Confirmed genuine. The runtime explicitly handles these eight action strings via an `if` check and `switch/case` (effects.mjs lines 231, 239-257). Although the actions originate from `data-action` HTML attributes (template-driven), the method itself is a closed dispatch with a well-defined set of recognized values. Using `string` loses the discriminant information that lets callers know what actions are valid."}],"module/applications/components/filigree-box.d.mts":[{"severity":"low","axis":"fidelity","member":"FiligreeBoxElement.tagName","fix":"Change `static tagName: string;` to `static tagName: \"filigree-box\";`","note":"low — unverified"},{"severity":"low","axis":"fidelity","member":"MaybeAdoptable","fix":"Change `declare const MaybeAdoptable: typeof HTMLElement;` to `declare const MaybeAdoptable: typeof foundry.applications.elements.AdoptableHTMLElement | typeof HTMLElement;`","note":"low — unverified"}],"module/applications/components/filter-state.d.mts":[{"severity":"low","axis":"shortcut","member":"constructor","fix":"Change `constructor(...args: any[])` to `constructor()` to match the base class signature.","note":"The base class AbstractFormInputElement is typed with `constructor()` (no parameters) in fvtt-types (form-element.d.mts line 13). The runtime FilterStateElement uses `constructor(...args)` only to pass through to super, adding no new parameters of its own. Using `...args: any[]` is a loose catch-all that bypasses the base constructor's no-parameter contract. ProficiencyCycleElement uses `constructor()` as the correct pattern. The shortcut finding is genuine."}],"module/applications/components/identifier-input.d.mts":[{"severity":"low","axis":"coverage","member":"_refresh","fix":"Add `protected _refresh(): void;` to the class.","note":"The runtime at line 140 overrides `_refresh()`. The base class declares `protected _refresh(): void` in fvtt-types (form-element.d.mts line 161), which is an identical signature. The omission is a real coverage gap but consumers see the correct signature from the base regardless. Severity downgraded from high to low."}],"module/applications/components/inventory.d.mts":[{"severity":"low","axis":"fidelity","member":"app","fix":"Change the return type of `get app()` from `foundry.applications.api.ApplicationV2` to `foundry.applications.api.ApplicationV2.Any`.","note":"Confirmed genuine. The runtime stores whatever `foundry.applications.instances.get(...)` returns. All three peer components that also expose `get app()` — ActivitiesElement (line 12), EffectsElement (line 14), and ItemListControlsElement (line 16) — use `foundry.applications.api.ApplicationV2.Any`. Using the bare non-generic class rather than the `.Any` alias is inconsistent with the project standard and prevents use in generic contexts."}],"module/applications/components/proficiency-cycle.d.mts":[{"severity":"low","axis":"fidelity","member":"tagName","fix":"Change `static tagName: string;` to `static tagName: \"proficiency-cycle\";`","note":"low — unverified"},{"severity":"low","axis":"coverage","member":"CSS","fix":"Add `/** @fires change */` (or equivalent) to the class-level JSDoc comment so the emitted change event is documented.","note":"low — unverified"}],"module/applications/currency-manager.d.mts":[{"severity":"low","axis":"fidelity","member":"deductActorCurrency","fix":"Replace the inline `{ recursive?: boolean; priority?: 'high' | 'low'; exact?: boolean; makeChange?: boolean }` with `dnd5e.types.defs.applications.CurrencyUpdateOptions`","note":"The inline options object duplicates `dnd5e.types.defs.applications.CurrencyUpdateOptions` exactly (all four fields with matching types and optionality). The runtime imports `CurrencyUpdateOptions` from `_types.mjs` and the generated interface is in `src/_gen/applications/_types.d.mts`. Using the named type improves maintainability and is the strictness mandate for this project."},{"severity":"low","axis":"fidelity","member":"getActorCurrencyUpdates","fix":"Replace the inline `{ recursive?: boolean; priority?: 'high' | 'low'; exact?: boolean; makeChange?: boolean }` with `dnd5e.types.defs.applications.CurrencyUpdateOptions`","note":"Same as `deductActorCurrency`: the inline options object is identical to `dnd5e.types.defs.applications.CurrencyUpdateOptions`. The runtime imports and uses the same named typedef from `_types.mjs`. The generated named interface exists and is reachable, so the inline duplication is a genuine fidelity shortcut."}],"module/applications/item/config/starting-equipment-config.d.mts":[{"severity":"low","axis":"coverage","member":"DEFAULT_OPTIONS","fix":"Add `static override DEFAULT_OPTIONS: foundry.applications.api.ApplicationV2.Configuration;` to the class body.","note":"Runtime declares static DEFAULT_OPTIONS at line 10. This static is absent from the declaration. While DEFAULT_OPTIONS carries no type-level API meaning for consumers, the project includes it in some other declarations (e.g., roll-configuration-dialog.d.mts declares static PARTS). A genuine but low-impact coverage gap."},{"severity":"low","axis":"coverage","member":"PARTS","fix":"Add `static override PARTS: Record<string, foundry.applications.api.HandlebarsApplicationMixin.PartConfiguration>;` to the class body.","note":"Runtime declares static PARTS at line 23 with a config template part. The declaration omits it. Same reasoning as DEFAULT_OPTIONS: low consumer impact but a genuine omission. The type `HandlebarsApplicationMixin.PartConfiguration` (or `HandlebarsTemplatePart`) is the correct type used in roll-configuration-dialog.d.mts."}],"module/applications/item/create-scroll-dialog.d.mts":[{"severity":"low","axis":"shortcut","member":"import Application5e from \"../api/application.mjs\"","fix":"Remove the unused import on line 4. The runtime only imports Dialog5e; Application5e is never referenced in the file body.","note":"Application5e appears only in the import statement (line 4) and nowhere else in the file. The runtime counterpart also does not import it. This is genuine dead code."}],"module/applications/item/item-sheet.d.mts":[{"severity":"low","axis":"fidelity","member":"RenderContext.name","fix":"Change `name?: { value: string; editable: unknown; field?: unknown }` to `name?: { value: string; editable: string; field?: foundry.data.fields.DataField.Any | null }` in `ItemSheet5e.RenderContext`.","note":"Confirmed: runtime lines 441-448 in `_prepareHeaderContext` show `editable` is assigned from `this.item.system._source.unidentified.name` or `this.item._source.name` (both plain strings), and `field` is assigned from `schema.getField(...)` which returns `DataField.Any | null`. Both more precise types clearly exist and are reachable."},{"severity":"low","axis":"fidelity","member":"RenderContext.img","fix":"Change `img?: { value: string; editable: unknown }` to `img?: { value: string; editable: string }` in `ItemSheet5e.RenderContext`.","note":"Confirmed: runtime line 451 assigns `editable: this.item._source.img`, which is a plain string. Using `unknown` is unnecessarily loose when the type is clearly `string`."},{"severity":"low","axis":"fidelity","member":"RenderContext.elements","fix":"Change `elements?: unknown;` to `elements?: { activities: string; effects: string };` in `ItemSheet5e.RenderContext`.","note":"Confirmed: runtime `DEFAULT_OPTIONS.elements` (lines 37-40) is `{ activities: 'dnd5e-activities', effects: 'dnd5e-effects' }`, and line 182 assigns `context.elements = this.options.elements`. The concrete shape `{ activities: string; effects: string }` is evident and `unknown` provides no type safety here."}],"module/applications/journal/config/compendium-toc-config.d.mts":[{"severity":"low","axis":"shortcut","member":"compendium","fix":"Change `CompendiumCollection<any>` to `CompendiumCollection.Any` in both `get compendium(): CompendiumCollection<any>` and `Configuration.compendium: CompendiumCollection<any> | null`. The `| null` in Configuration is correct because DEFAULT_OPTIONS sets `compendium: null`, so keep it as `CompendiumCollection.Any | null`.","note":"The runtime JSDoc types this as `{CompendiumCollection}` (no generic). The project-wide convention for opaque compendium references is `CompendiumCollection.Any`, as consistently used in peer files `journal-toc-config.d.mts`, `table-of-contents.d.mts`, etc. Using `CompendiumCollection<any>` is a shortcut deviation from that convention. Severity is low (not high) because the structural effect is equivalent — it does not break type checking in practice."}],"module/applications/journal/config/journal-navigation-config.d.mts":[{"severity":"low","axis":"shortcut","member":"<file>","fix":"Remove `import Application5e from \"../../api/application.mjs\";` from line 3.","note":"Confirmed: `Application5e` is imported on line 3 but never referenced anywhere in the file. The class extends `DocumentSheet5e` and all namespace interfaces extend `DocumentSheet5e.*` variants. This is a dead import that adds noise without purpose."}],"module/applications/journal/journal-entry-sheet.d.mts":[{"severity":"low","axis":"coverage","member":"_attachFrameListeners","fix":"Add `protected override _attachFrameListeners(): void;` to the class body.","note":"Confirmed: runtime (line 93-99) overrides _attachFrameListeners to attach a pointerdown handler for middle-click prevention. The base signature is declared in foundry's ApplicationV2 as `protected _attachFrameListeners(): void`. Genuine coverage gap."}],"module/applications/journal/map-page-sheet.d.mts":[{"severity":"low","axis":"coverage","member":"_onRender","fix":"Add `protected override _onRender(context: RenderContext, options: RenderOptions): Promise<void>;` to the class body.","note":"The runtime clearly overrides `async _onRender(context, options)` with non-trivial DOM mutation logic (conditional on `this.isView`, element creation, DOM insertion). It is marked `@inheritDoc` but is a real, substantive override. The declaration omits it entirely. Genuine coverage gap, low severity since the base signature still applies."}],"module/applications/journal/spells-page-sheet.d.mts":[{"severity":"low","axis":"coverage","member":"static EDIT_PARTS","fix":"Add `static EDIT_PARTS: Record<string, foundry.applications.api.HandlebarsApplicationMixin.HandlebarsTemplatePart>;` to the class body. Use HandlebarsTemplatePart rather than the inline object shape the finder proposed, to match the project's existing PARTS typing convention.","note":"Runtime line 31 declares `static EDIT_PARTS` as an override with specific handlebars template parts. Absent from the declaration. The finder's proposed fix uses an inline shape; the project convention (see application-v2-mixin.d.mts) uses HandlebarsApplicationMixin.HandlebarsTemplatePart."},{"severity":"low","axis":"coverage","member":"static VIEW_PARTS","fix":"Add `static VIEW_PARTS: Record<string, foundry.applications.api.HandlebarsApplicationMixin.HandlebarsTemplatePart>;` to the class body.","note":"Runtime line 46 declares `static VIEW_PARTS` as an override with a single `content` entry. Absent from the declaration. Same HandlebarsTemplatePart typing convention applies."}],"module/applications/journal/spells-unlinked-config.d.mts":[{"severity":"low","axis":"fidelity","member":"RenderContext.spellLevelOptions","fix":"Change the type to `Array<{ value: dnd5e.types.SpellLevel.TypeKey; label: string }>`. This is assignable to `FormSelectOption[]` so downstream Foundry helpers still accept it, and it carries the precise domain key union.","note":"Confirmed genuine under the strictness mandate. The runtime builds entries via `Object.entries(CONFIG.DND5E.spellLevels)`, whose keys are `dnd5e.types.SpellLevel.TypeKey` (the string-numeral union '0'|'1'|...|'9'). `dnd5e.types.SpellLevel.TypeKey` is defined in `src/module/config/spellLevels.d.mts` and is plainly reachable. `FormSelectOption` types `value` as `string | undefined`, which is looser than necessary. Severity is low rather than medium because `FormSelectOption[]` is structurally compatible — no runtime behaviour is wrong, only a missed narrowing opportunity."},{"severity":"low","axis":"fidelity","member":"RenderContext.spellSchoolOptions","fix":"Change the type to `Array<{ value: dnd5e.types.SpellSchool.TypeKey; label: string }>`. This is a structural subtype of `FormSelectOption[]` and carries the precise expandable school key union.","note":"Confirmed genuine. Runtime builds entries via `Object.entries(CONFIG.DND5E.spellSchools)`, whose keys are `dnd5e.types.SpellSchool.TypeKey` (the 'abj'|'con'|'div'|'enc'|'evo'|'ill'|'nec'|'trs' union, expandable via `OverrideTypes`). The precise type exists in `src/module/config/spellSchools.d.mts`. Same reasoning as spellLevelOptions: the current `FormSelectOption[]` is compatible but loses the domain key union. Severity downgraded from medium to low for the same structural-compatibility reason."}],"module/applications/journal/table-of-contents.d.mts":[{"severity":"low","axis":"coverage","member":"_onDragStart","fix":"Add `protected _onDragStart(event: DragEvent): void;` to the class body","note":"Runtime lines 225-231 override `_onDragStart` with custom drag logic calling `_getEntryDragData`. The .d.mts omits this override entirely."}],"module/applications/region-behavior/rotate-area-config.d.mts":[{"severity":"low","axis":"fidelity","member":"RenderContext.positions","fix":"Replace `data: object` with `data: { angle: number }` in the positions array element type. The runtime maps `context.source.system.positions[index]` directly as `data`, and the schema in rotate-area.mjs defines each element as `new SchemaField({ angle: new NumberField({ required: true, nullable: false, initial: 0, min: -360, max: 360 }) })`, so the source shape is precisely `{ angle: number }`.","note":"Genuine fidelity gap. The runtime schema (RotateAreaRegionBehaviorType.defineSchema positions field) fully specifies each element as a SchemaField containing only a NumberField named `angle`. The precise shape is reachable and `object` hides it from consumers."}],"module/applications/roll-table-sheet.d.mts":[{"severity":"low","axis":"shortcut","member":"changeMode","fix":"Replace `PrimarySheetMixin.ModeValue` with a local type alias (e.g. `1 | 2`) or re-export `PrimarySheetMixin.ModeValue` under a neutral name, and remove the `PrimarySheetMixin` import from roll-table-sheet.d.mts.","note":"Confirmed genuine shortcut. RollTableSheet5e does not extend PrimarySheetMixin, yet the file imports the mixin function solely to access its namespace type ModeValue. The runtime JSDoc (line 68) references PrimarySheetMixin.MODES for the parameter, so the type itself is semantically correct (1 | 2), but importing a mixin constructor to borrow a namespace type creates a misleading implied relationship. Severity kept at low — type is correct, only the import source is misleading."}],"module/applications/settings/base-settings.d.mts":[{"severity":"low","axis":"fidelity","member":"RenderContext.buttons","fix":"Change to `buttons: foundry.applications.api.ApplicationV2.FormFooterButton[];`","note":"The runtime uses `{ type: 'submit', icon: '...', label: '...' }` against the Foundry `form-footer.hbs` template, which is typed by `ApplicationV2.FormFooterButton`. The current bare `string` for `type` is weaker than `HTMLButtonElement['type']` (`'button' | 'reset' | 'submit'`). Severity is low (not medium) because the only concrete value used at runtime is `'submit'`, so consumers are unlikely to be broken in practice, but referencing the Foundry interface is strictly more correct. The optionality difference (`icon?`/`label?` in Foundry vs required here) is also a mild correctness gap; the Foundry interface should be used directly."}],"module/applications/settings/calendar-settings.d.mts":[{"severity":"low","axis":"fidelity","member":"RenderContext.message","fix":"Change `message?: { level: string; text: string }` to `message?: { level: \"info\" | \"warn\" | \"error\"; text: string }`","note":"low — unverified"}],"module/applications/settings/combat-settings.d.mts":[{"severity":"low","axis":"correctness","member":"CombatSettingsConfig.RenderContext.legend","fix":"Change `legend: string;` to `legend?: string;` in `CombatSettingsConfig.RenderContext`.","note":"The runtime `_preparePartContext` only assigns `context.legend` inside named case branches (initiative, criticals, npcs, encounterPlacement). The footer part falls through with no match, so `legend` is never set for that render context. The base class also does not initialize it. Typing it as required `string` is incorrect; the field is absent for the footer part and the template confirms optional usage via `{{ifThen legend legend (localize ...)}}`."}],"module/applications/shared/movement-senses-config.d.mts":[{"severity":"low","axis":"fidelity","member":"RenderContext.travel","fix":"Replace `travel?: object` with `travel?: { data: object; extras: { field: foundry.data.fields.DataField.Any; value: unknown; localize: true; options?: { value: string; label: string }[] }[]; fields: Record<string, foundry.data.fields.DataField.Any>; unitsOptions: { value: string; label: string }[]; types: { label: string; pace: { field: foundry.data.fields.DataField.Any; name: string; placeholder: unknown; value: unknown }; speed: { field: foundry.data.fields.DataField.Any; name: string; placeholder: unknown; value: unknown } }[] };`","note":"Runtime lines 165-195 build a rich, fully inspectable `context.travel` structure with `data`, `extras`, `fields`, `unitsOptions`, and `types`. Each sub-property has a knowable shape from the source. Bare `object` prevents any sub-property access at the call site. Severity low is appropriate since travel is a secondary rendering concern, but the finding is genuine."}],"module/applications/shared/source-config.d.mts":[{"severity":"low","axis":"fidelity","member":"RenderContext.rulesVersions","fix":"Change `{ value: string; label: string }[]` to `{ value: \"\" | \"2024\" | \"2014\"; label: string }[]`.","note":"Runtime lines 55-59 hardcode exactly three values: `\"\"`, `\"2024\"`, and `\"2014\"`. The `\"\"` entry is a UI sentinel for 'no selection'. While there is no single named type alias for this three-way union in the repo, the values are fixed and knowable from the runtime. The proposed fix correctly includes `\"\"` in the union (the `SourceField.Schema.rules` initial type only covers `\"2024\" | \"2014\"`, so the full union must be hand-written). Genuine fidelity improvement; the bare `string` is too loose."}],"module/applications/tabs.d.mts":[{"severity":"low","axis":"seam","member":"Tabs5e","fix":"Add `declare namespace Tabs5e { interface Any extends Tabs5e {} interface AnyConstructor extends fvttUtils.Identity<typeof Tabs5e> {} }` after the class declaration, matching the ChatLog5e pattern.","note":"The namespace is genuinely absent, and ChatLog5e (a structurally parallel simple utility subclass) does have it — so it is a real inconsistency. However, the claim that 'every other exported class' has this seam is false: ContextMenu5e (another simple Foundry UX subclass at the same layer) also lacks the namespace. No usage of Tabs5e.Any or Tabs5e.AnyConstructor exists anywhere in the codebase, and the class has no subclasses or generic-constraint consumers. The gap is a minor consistency issue rather than a blocking seam problem, so severity is downgraded from medium to low."}],"_gen/applications/calendar/_types.d.mts":[{"severity":"low","axis":"correctness","member":"CalendarHUDButton.dataset","fix":"Change `dataset?: Record<string, unknown>` to `dataset?: object` to match the JSDoc source (`{object}`) and the hand-crafted CalendarHUD.CalendarHUDButton interface in src/module/applications/calendar/calendar-hud.d.mts.","note":"The runtime JSDoc declares `{object}` and the hand-crafted canonical interface uses `object`. The generator emitted `Record<string, unknown>` instead, which is structurally narrower (excludes non-string-keyed objects). The divergence is real, but severity is low because in practice `dataset` on HTML elements always has string keys, so `Record<string, unknown>` would rarely cause a consumer-visible error. Nonetheless, the generated type should match its two authoritative sources."}],"_gen/applications/journal/_types.d.mts":[{"severity":"low","axis":"seam","member":"JournalEditorConfiguration","fix":"Either have `JournalEditor.Configuration` extend `dnd5e.types.defs.applications.journal.JournalEditorConfiguration` (so declaration-merging on the generated interface propagates), or drop the generated interface entirely and rely solely on the hand-written `JournalEditor.Configuration`.","note":"Confirmed genuine as a dead-type seam issue. The generated `JournalEditorConfiguration` interface lives in `dnd5e.types.defs.applications.journal` but is never referenced by the hand-written `JournalEditor.Configuration`, which independently defines `textKeyPath`. Declaration-merging against the generated interface has no effect on the actual type graph. The generated and hand-written interfaces are entirely disconnected, making the generated typedef useless."}],"_gen/core/_types.d.mts":[{"severity":"low","axis":"shortcut","member":"ActivityActivationAutoConsumptionConfiguration.canConsume","fix":"Once the Activity document class is declared, replace `fvttUtils.AnyObject` with `dnd5e.types.Activity.Instance` and remove the TODO comment.","note":"Confirmed genuine shortcut. The runtime `_types.mjs` lines 43-46 define a `@callback ActivityActivationAutoConsumptionPredicate` with `@property {Activity} activity`, confirming the parameter should be an Activity instance. The generated declaration uses `fvttUtils.AnyObject` with an explicit `// TODO` comment acknowledging the placeholder. This is a deliberate temporary loose type pending the Activity document being ported, not a false positive."}],"_gen/data/actor/fields/_types.d.mts":[{"severity":"low","axis":"fidelity","member":"TravelData.units","fix":"After applying the correctness fix, change the type to `dnd5e.types.TravelUnit.TypeKey | null;`. The runtime at travel-field.mjs:67 accesses `CONFIG.DND5E.travelUnits[units]?.type`, confirming the field stores a travelUnits config key. dnd5e.types.TravelUnit.TypeKey (\"mph\"|\"kph\") is the expandable seam-A union for this domain.","note":"Confirmed genuine: travel-field.mjs line 67 uses units as a key into CONFIG.DND5E.travelUnits, and dnd5e.types.TravelUnit.TypeKey exists in travelUnits.d.mts. Low severity is appropriate because nullable makes this slightly more complex and TravelUnit.OverrideTypes allows extension."}],"_gen/data/chatMessage/fields/_types.d.mts":[{"severity":"low","axis":"correctness","member":"ActorUpdatesDescription.actor","fix":"Change `actor: Record<string, unknown>;` to `actor: object;` to match the runtime JSDoc (`{object}`) and the hand-crafted deltas-field.d.mts which types the `updates` parameter as `object`.","note":"The JSDoc in _types.mjs line 15 declares `@property {object} actor`. The hand-crafted deltas-field.d.mts passes `updates: object` to `getDeltas`. The generator enriched this to `Record<string, unknown>` which is inconsistent with both the JSDoc source and the hand-crafted file. This is a genuine inconsistency, though severity is low because `Record<string, unknown>` is assignable to `object` and does not break consumers."},{"severity":"low","axis":"shortcut","member":"ActorUpdatesDescription.create","fix":"Change `create?: Array<Record<string, unknown> & { _id: string }>;` to `create?: object[];` to match the JSDoc source (`{object[]}`). The runtime at line 35 only calls `Array.from(updates.create)` with no `_id` access on create entries, confirming `_id` is not required for this array.","note":"The JSDoc in _types.mjs line 16 declares `@property {object[]} [create]`. The runtime in deltas-field.mjs line 35 uses `Array.from(updates.create)` without accessing `_id` on create entries (unlike `item` entries). The `_id` annotation was erroneously carried over from `item` context. The generator exceeded its JSDoc source by adding `& { _id: string }`, making this a genuine shortcut/enrichment-beyond-source."}],"_gen/data/item/_types.d.mts":[{"severity":"low","axis":"fidelity","member":"ToolItemSystemData.proficient","fix":"Change `proficient: number` to `proficient: dnd5e.types.ProficiencyLevel.TypeKey | null`.","note":"Confirmed: tool.mjs lines 56-58 define `NumberField({ required: true, initial: null, min: 0, max: 2, step: 0.5 })`. Values are 0, 0.5, 1, 2 (matching `ProficiencyLevel.TypeKey`) or null. Bare `number` is too wide."},{"severity":"low","axis":"fidelity","member":"WeaponItemSystemData.proficient","fix":"Change `proficient: number` to `proficient: 0 | 1 | null`.","note":"Confirmed: weapon.mjs lines 72-74 define `NumberField({ required: true, min: 0, max: 1, integer: true, initial: null })`. Only 0 or 1 (integer in [0,1]) or null. Does not use the full ProficiencyLevel domain (no 0.5 or 2)."},{"severity":"low","axis":"fidelity","member":"EquipmentItemSystemData.proficient","fix":"Change `proficient: number` to `proficient: 0 | 1 | null`.","note":"Confirmed: equipment.mjs lines 61-63 define `NumberField({ required: true, min: 0, max: 1, integer: true, initial: null })`. Same constraints as weapon — only 0, 1, or null."}],"_gen/documents/advancement/_types.d.mts":[{"severity":"low","axis":"shortcut","member":"AdvancementMetadata.apps.config","fix":"Port/declare AdvancementConfig in src/ (from _research/dnd5e/module/applications/advancement/advancement-config-v2.mjs) and replace `fvttUtils.AnyConstructor` with `typeof AdvancementConfig`.","note":"The TODO comment in the generated file explicitly acknowledges this is a placeholder: '// TODO: typeof AdvancementConfig (not yet ported to src/)'. The runtime advancement.mjs imports AdvancementConfig from advancement-config-v2.mjs and uses it as the apps.config value. The placeholder is a genuine shortcut pending that port."},{"severity":"low","axis":"shortcut","member":"AdvancementMetadata.apps.flow","fix":"Port/declare AdvancementFlow in src/ (from _research/dnd5e/module/applications/advancement/advancement-flow-v2.mjs) and replace `fvttUtils.AnyConstructor` with `typeof AdvancementFlow`.","note":"The TODO comment in the generated file explicitly acknowledges this is a placeholder: '// TODO: typeof AdvancementFlow (not yet ported to src/)'. The runtime advancement.mjs imports AdvancementFlow from advancement-flow-v2.mjs and uses it as the apps.flow value. The placeholder is a genuine shortcut pending that port."}],"module/_api/applications-root.d.mts":[{"severity":"low","axis":"coverage","member":"dnd5e.applications.fields.createIdentifierInput","fix":"Add `const createIdentifierInput: typeof import('../applications/fields.mjs').createIdentifierInput;` inside the `namespace dnd5e.applications { namespace fields { … } }` block in applications-api.d.mts (the file that owns the fields sub-namespace).","note":"Confirmed genuine. The runtime fields.mjs exports createIdentifierInput (line 29) and fields.d.mts fully types it, but applications-api.d.mts only surfaces four of the five exported functions in namespace dnd5e.applications.fields — createIdentifierInput is absent. The fix belongs in applications-api.d.mts, not applications-root.d.mts, but the gap is real."}],"module/canvas/api/base-placement.d.mts":[{"severity":"low","axis":"shortcut","member":"place (static)","fix":"Replace the `object[]` fallback with `never`: `Promise<InstanceType<This> extends BasePlacement<any, infer D> ? D[] : never>`","note":"Because `This extends typeof BasePlacement`, `InstanceType<This>` always extends `BasePlacement<any, any>`, so the conditional's false branch (`object[]`) is structurally unreachable. Using `never` makes the dead branch explicit and avoids implying that the method could legitimately return `object[]`. The fix is a minor cleanup — it does not affect real-world call sites — so severity is low rather than medium."}],"module/canvas/token.d.mts":[{"severity":"low","axis":"fidelity","member":"_drawHPBar","fix":"Change data parameter type from object to NonNullable<TokenDocument.GetBarAttributeReturn>","note":"The named type NonNullable<TokenDocument.GetBarAttributeReturn> exists and is reachable in fvtt-types. _drawBar (base) passes exactly that type to _drawHPBar. While the runtime JSDoc loosely annotates it as {object} and the body ignores the parameter in favour of this.document.actor.system.attributes.hp, the declared interface should match the actual call site type for type safety on any override. Genuine fidelity gap, but low severity since the parameter is effectively unused."}],"module/canvas/token-placement.d.mts":[{"severity":"low","axis":"correctness","member":"adjustAppendedNumber","fix":"Change the first parameter type to `globalThis.TokenDocument.Implementation | { name?: string | null }` to reflect the union the runtime JSDoc declares.","note":"The runtime JSDoc explicitly documents `@param {TokenDocument|object}` and the implementation has an explicit `instanceof TokenDocument` branch to handle plain objects by direct property assignment. The declaration drops this union and types only `TokenDocument.Implementation`. However, all known callers (group.mjs line 83, summon.mjs line 547) pass actual TokenDocument instances obtained via `actor.getTokenDocument()`, so the plain-object path is never exercised in practice. The omission is real but severity is low — no caller is currently broken by the narrower type; only future callers using the plain-object path would be affected."}],"module/dice/basic-roll.d.mts":[{"severity":"low","axis":"shortcut","member":"DefaultConfigurationDialog","fix":"Once RollConfigurationDialog is declared, replace `fvttUtils.AnyConstructor` with `typeof RollConfigurationDialog`.","note":"Runtime assigns `RollConfigurationDialog` (a concrete class). The TODO comment confirms this is a deliberate placeholder pending the port of that application."}],"module/dice/d20-roll.d.mts":[{"severity":"low","axis":"coverage","member":"mergeOptions (static override)","fix":"Add `static override mergeOptions<T extends object>(original?: T, other?: object): T;` to the D20Roll class body.","note":"The runtime overrides mergeOptions at d20-roll.mjs:262-269 to additionally merge advantage, disadvantage, maximum, and minimum fields. The type-level signature (parameter types and return type) is unchanged from BasicRoll's declaration, so callers using D20Roll.mergeOptions still get the correct types. The behavioral change is invisible to the type system. Severity is low — not medium — because the override does not change the callable API signature, only internal merging behavior. Still worth declaring `override` for fidelity and correctness of subtype modelling."},{"severity":"low","axis":"coverage","member":"_prepareMessageData (static override)","fix":"Add `protected static override _prepareMessageData(rolls: D20Roll[], messageData: object): void;` to the D20Roll class body.","note":"The runtime overrides _prepareMessageData at d20-roll.mjs:186-203. The base declaration in basic-roll.d.mts incorrectly states return type `object` (the runtime base body is empty and returns void). D20Roll's override also returns void and narrows `rolls` to `D20Roll[]`. Declaring the override makes the narrowed parameter type and correct void return visible when calling through the D20Roll type. Severity is low because the method is protected and the behavioral difference is not reachable via normal typed call sites."}],"module/dice/_types.d.mts":[{"severity":"low","axis":"correctness","member":"BasicRollConfigurationDialogOptions.default.rollMode","fix":"Change `rollMode?: number` to `rollMode?: CONST.DICE_ROLL_MODES` inside the `default?` sub-object","note":"The runtime JSDoc annotation `{number}` is wrong. In roll-configuration-dialog.mjs:236 this value is used as the initial value for a `StringField` whose options come from `CONFIG.ChatMessage.modes` keys (strings). Lines 381/393 also assign the field from `formData.get('rollMode')` which returns a string. The field is a string roll-mode key, not a numeric index."}],"module/drag-drop.d.mts":[{"severity":"low","axis":"fidelity","member":"dropEffect","fix":"Change `static dropEffect: \"copy\" | \"move\" | \"link\" | \"none\" | null;` to `static dropEffect: dnd5e.types.DropEffectValue | null;`.","note":"The runtime JSDoc at line 13 uses `{DropEffectValue|null}`. `dnd5e.types.DropEffectValue` exists in src/_gen/core/_types.d.mts line 170 as `\"copy\"|\"move\"|\"link\"|\"none\"`. The named type should be used per the strictness mandate rather than an inline union that will drift silently if the upstream definition changes."}],"module/enrichers.d.mts":[{"severity":"low","axis":"shortcut","member":"parseConfig","fix":"Add two overload signatures: `parseConfig(match?: string, options?: { multiple?: false }): object` and `parseConfig(match: string, options: { multiple: true }): object[]`, keeping the union implementation signature. This allows callers with a literal multiple:true/false to get the correct narrowed return type.","note":"Confirmed: line 114 of enrichers.mjs shows the runtime returns an array only when multiple is true and a plain object otherwise. The current return type object | object[] forces callers to manually narrow. Overloads are the standard TypeScript pattern for this two-branch behavior and would avoid the union-return shortcut. Real but low-severity since the function is mostly called internally."}],"module/migration.d.mts":[{"severity":"low","axis":"shortcut","member":"migrateCompendium","fix":"Change `incrementProgress?: Function` to `incrementProgress?: () => void`","note":"The runtime calls `incrementProgress?.()` with no arguments and discards the return value (line 297). The bare `Function` type is a known shortcut per the strictness mandate; `() => void` is the precise signature demonstrated by the usage. Axis changed from fidelity to shortcut because `Function` is a deliberate placeholder type, not a fidelity mistake about a domain value — this fits the shortcut axis (loose type used where a precise signature exists)."}],"module/settings.d.mts":[{"severity":"low","axis":"correctness","member":"SettingConfig[\"dnd5e.defaultSkills\"]","fix":"Change to `SetField<dnd5e.types.fields.RestrictedStringField<dnd5e.types.Skill.TypeKey>>` (omit the options argument, using the field's defaults) so the element field options match the runtime StringField.","note":"low — unverified"}],"module/utils.d.mts":[{"severity":"low","axis":"fidelity","member":"prepareFormulaValue","fix":"Change `rollData: object` to `rollData: dnd5e.types.defs.documents.RollData`","note":"The named interface `dnd5e.types.defs.documents.RollData` exists in src/_gen/documents/_types.d.mts and the runtime imports and documents `@param {RollData} rollData`. The interface is currently empty, so there is no runtime correctness difference yet, but using the named type follows the project's convention and enables future extension without touching this signature. Real but low severity."}]}