import NPCActorSheet from "../npc-sheet.mjs";

/**
 * An Actor sheet for NPCs.
 * @deprecated Use NPCActorSheet instead
 */
declare class ActorSheet5eNPC extends NPCActorSheet {
  /** @privateRemarks All deprecated classes should accept anything for its constructor. */
  constructor(...args: any[]);
}

/**
 * An Actor sheet for NPCs.
 * @deprecated Use NPCActorSheet instead
 */
declare class ActorSheet5eNPC2 extends NPCActorSheet {
  /** @privateRemarks All deprecated classes should accept anything for its constructor. */
  constructor(...args: any[]);
}

export { ActorSheet5eNPC, ActorSheet5eNPC2 };
