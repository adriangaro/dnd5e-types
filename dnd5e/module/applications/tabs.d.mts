// TODO: replace with foundry.applications?.ux?.Tabs
/**
 * A specialized subclass of Tabs that handles tabs which exist outside an Application's inner HTML.
 */
export default class Tabs5e extends Tabs {}
